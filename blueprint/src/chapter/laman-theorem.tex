\section{Laman's theorem}
\label{sec:laman-theorem}

For $n \ge 2$ vertices, a simple graph is generically rigid in the
plane iff it contains a Laman spanning subgraph (a $(2, 3)$-tight
subgraph on the same vertex set). The two directions land separately:

\begin{itemize}
  \item \emph{$\Leftarrow$ (every Laman graph is generically rigid in dim 2).}
    A Henneberg induction on $|V|$. Base case $|V| = 2$: $K_2$ is
    generically rigid (\cref{thm:top-fin-two-isGenericallyRigidInj}).
    Inductive step: peel off a Type~I or Type~II move via
    \cref{thm:isLaman-exists-typeI-or-typeII-reverse} and apply the
    move-rigidity-preservation theorem
    (\cref{thm:typeI-isGenericallyRigidInj-two} or
    \cref{thm:typeII-isGenericallyRigidInj-two}). The induction
    maintains the strong predicate \emph{injectively} generically rigid
    (\cref{def:isGenericallyRigidInj}), then weakens at the end.
  \item \emph{$\Rightarrow$ (every generically rigid graph contains a
    Laman spanning subgraph).} The Lov\'asz--Yemini~\cite{lovaszYemini1982}
    direction, which uses the easy half of the identification of the
    rigidity matroid in dim 2 with the $(2, 3)$-count matroid:
    row-independence of an edge set at a generic placement implies
    $(2, 3)$-sparsity of the underlying subgraph.
\end{itemize}

\subsection{The \texorpdfstring{$d = 2$}{d=2} edge-count corollary}

\begin{lemma}[Edge-count bound in dim 2]
  \label{lem:isGenericallyRigid-card-mul-le-two}
  \lean{SimpleGraph.IsGenericallyRigid.card_mul_le_two}
  \leanok
  \uses{thm:isGenericallyRigid-card-mul-le}
  Let $V$ be finite. If $G$ is generically rigid in dimension $2$, then
  \[
    2 |V| \;\le\; |E(G)| + 3.
  \]
\end{lemma}
\begin{proof}
  \leanok
  Specialize \cref{thm:isGenericallyRigid-card-mul-le} at $d = 2$.
\end{proof}

\subsection{The \texorpdfstring{$\Leftarrow$}{<=} direction}

\begin{theorem}[Every Laman graph is generically rigid in dim 2]
  \label{thm:isLaman-isGenericallyRigid-two}
  \lean{SimpleGraph.IsLaman.isGenericallyRigid_two}
  \leanok
  \uses{def:isLaman, def:isGenericallyRigid, def:isGenericallyRigidInj,
    thm:isLaman-exists-typeI-or-typeII-reverse,
    thm:typeI-isGenericallyRigidInj-two,
    thm:typeII-isGenericallyRigidInj-two,
    thm:top-fin-two-isGenericallyRigidInj,
    lem:isLaman-eq-top-of-card-eq-two,
    lem:isGenericallyRigidInj-iso}
  If $G$ is a Laman graph on a finite vertex type $V$, then $G$ is
  generically rigid in dimension $2$.
\end{theorem}
\begin{proof}
  \leanok
  Strong induction on $|V|$, proving the stronger statement: every
  Laman graph is injectively generically rigid in dimension $2$
  (\cref{def:isGenericallyRigidInj}); the actual theorem then weakens
  to plain generic rigidity at the end.

  \emph{Base case ($|V| \le 2$).} Tightness $|E(G)| + 3 = 2 |V|$ is
  infeasible in $\N$ for $|V| \le 1$, so $|V| = 2$ and by
  \cref{lem:isLaman-eq-top-of-card-eq-two} we have $G = K_2$ (on $V$).
  Transport \cref{thm:top-fin-two-isGenericallyRigidInj} at dimension
  $2$ along the bijection $V \simeq \mathrm{Fin}\,2$ using
  \cref{lem:isGenericallyRigidInj-iso}.

  \emph{Inductive step ($|V| \ge 3$).} By
  \cref{thm:isLaman-exists-typeI-or-typeII-reverse} pick $v$ and a
  Laman $G'$ on $\{w \ne v\}$ such that
  $G \simeq \mathrm{typeI}\,G'\,a\,b$ or
  $G \simeq \mathrm{typeII}\,G'\,a\,b\,c$. The vertex type
  $\{w \ne v\}$ has cardinality $|V| - 1$, so the induction hypothesis
  applies to $G'$: $G'$ is injectively generically rigid in dim $2$.
  Applying the appropriate move-preservation theorem
  (\cref{thm:typeI-isGenericallyRigidInj-two} or
  \cref{thm:typeII-isGenericallyRigidInj-two}) gives the same for
  $\mathrm{typeI}\,G'\,a\,b$ or $\mathrm{typeII}\,G'\,a\,b\,c$, and
  iso transport (\cref{lem:isGenericallyRigidInj-iso}) carries the
  conclusion back to $G$.
\end{proof}

\subsection{The \texorpdfstring{$\Rightarrow$}{=>} direction}

The statement we are proving is the necessary direction of Laman's
original theorem~\cite{laman1970}: every graph generically rigid in
the plane contains a Laman spanning subgraph. We work in the
linear-algebraic formulation of infinitesimal rigidity due to
Asimow--Roth~\cite{asimowRoth1978}: a row-basis of the rigidity
matrix at a sufficiently generic placement supports the desired Laman
subgraph.

We isolate the row-independence relation between an edge set and a
placement, then prove two facts about it: (a)~row-independence implies
the $(2, 3)$-sparsity required by the Laman definition (a classical
Maxwell-type counting argument; see Jord\'an~\cite{jordan2016} Lemma
1.3.1 for the modern presentation we follow); and (b)~a row-basis of
size $2|V| - 3$ exists whenever $G$ is generically rigid (rank lower
bound + linear-algebra extension). Combining (a) and (b) gives a
$(2, 3)$-tight spanning subgraph, i.e.\ a Laman subgraph.

The two facts together amount to the ``easy'' direction of an
identity due to Lov\'asz--Yemini~\cite{lovaszYemini1982}: in
dimension $2$, an edge set is row-independent at a generic placement
if and only if its underlying subgraph is $(2, 3)$-sparse. The harder
converse direction of that identity --- $(2, 3)$-sparse
$\Rightarrow$ row-independent --- is \emph{not} needed for the iff
and is not formalized.

\subsubsection{Row-independence in the rigidity matroid}

\begin{definition}[Row-independent edge set at a placement]
  \label{def:edgeSet-rowIndependent}
  \lean{SimpleGraph.EdgeSetRowIndependent}
  \leanok
  \uses{def:rigidityMap}
  Let $G : \mathrm{SimpleGraph}\,V$, let $p : V \to \R^{d}$ be a
  framework, and let $I \subseteq E(G)$. We say that $I$ is
  \emph{row-independent at $p$} if the family of edge-rows
  $\bigl(\mathrm{RigidityMap}\,G\,p\,(\cdot)\,e\bigr)_{e \in I}$,
  viewed as linear functionals on $\mathrm{Framework}\,V\,d$, is
  linearly independent over $\R$.

  Equivalently, the composition of $\mathrm{RigidityMap}\,G\,p$ with
  the coordinate projection $E(G) \to \R \;\to\; I \to \R$ has full
  rank $|I|$.
\end{definition}

\begin{remark}
  The row-independent edge sets form the independent sets of a
  matroid on $E(G)$ --- the \emph{rigidity matroid} of $(G, p)$. We
  do not package it as a \texttt{Mathlib.Combinatorics.Matroid}
  object: the (\(\Rightarrow\)) direction of Laman's theorem
  needs only the independence relation and its two named facts
  below, and the $\mathrm{Matroid}$ packaging is reusable but
  off the critical path.
\end{remark}

\subsubsection{The rigidity rows as a family in the algebraic dual}

The basis-pick (\cref{lem:exists-rowIndependent-edge-basis}) works in
the algebraic dual $(\mathrm{Framework}\,V\,d)^{*}$ throughout, where
the row-rank-equals-column-rank identity
\texttt{LinearMap.finrank\_range\_dualMap\_eq\_finrank\_range} applies
directly. The function-module presentation of
\cref{def:edgeSet-rowIndependent} and the dual-module presentation
agree on linear independence, by linearity and injectivity of the
forgetful $(M \to_{\R} \R) \to (M \to \R)$ map.

\begin{definition}[Rigidity row at a placement]
  \label{def:rigidityRow}
  \lean{SimpleGraph.rigidityRow}
  \leanok
  \uses{def:rigidityMap}
  The \emph{rigidity row} indexed by an edge $e \in E(G)$ at a
  placement $p$ is the linear functional
  $\mathrm{rigidityRow}\,G\,p\,e \in (\mathrm{Framework}\,V\,d)^{*}$
  obtained by composing $\mathrm{RigidityMap}\,G\,p$ with the
  coordinate projection $E(G) \to \R \;\to\; \R$ at $e$. As a
  function, $\mathrm{rigidityRow}\,G\,p\,e\,(\dot p) =
  \mathrm{RigidityMap}\,G\,p\,(\dot p)\,e$.
\end{definition}

\begin{lemma}[Row span equals dualMap range]
  \label{lem:span-range-rigidityRow}
  \lean{SimpleGraph.span_range_rigidityRow}
  \leanok
  \uses{def:rigidityRow, def:rigidityMap}
  Let $V$ be finite. The $\R$-linear span of the rigidity rows
  $\{\mathrm{rigidityRow}\,G\,p\,e\}_{e \in E(G)}$ equals the range of
  $(\mathrm{RigidityMap}\,G\,p)^{*}$ (the algebraic transpose).
\end{lemma}
\begin{proof}
  \leanok
  Each row is the image under
  $(\mathrm{RigidityMap}\,G\,p)^{*}$ of the dual-basis element
  $b^{*}_{e}$ of $\R^{E(G)}$, and the dual basis spans the whole
  dual; hence the image of its range under
  $(\mathrm{RigidityMap}\,G\,p)^{*}$ is the range of that
  transpose.
\end{proof}

\begin{lemma}[Row-independence equals dual-module linear independence]
  \label{lem:edgeSetRowIndependent-iff-linearIndepOn-rigidityRow}
  \lean{SimpleGraph.edgeSetRowIndependent_iff_linearIndepOn_rigidityRow}
  \leanok
  \uses{def:edgeSet-rowIndependent, def:rigidityRow}
  An edge set $I \subseteq E(G)$ is row-independent at $p$ if and
  only if the family
  $\{\mathrm{rigidityRow}\,G\,p\,e\}_{e \in I}$ is linearly
  independent in $(\mathrm{Framework}\,V\,d)^{*}$.
\end{lemma}
\begin{proof}
  \leanok
  The forgetful map $(\mathrm{Framework}\,V\,d)^{*} \to
  (\mathrm{Framework}\,V\,d \to \R)$ is $\R$-linear and injective,
  so it transports linear independence in both directions; both
  sides reduce to the same linear-independence statement on the
  same family of underlying functions.
\end{proof}

\subsubsection{Rank of the rigidity map at a generically rigid placement}

\begin{lemma}[Rank lower bound at a generically rigid placement, d-general]
  \label{lem:rigidityMap-finrank-range-ge-of-isGenericallyRigid}
  \lean{SimpleGraph.rigidityMap_finrank_range_ge_of_isGenericallyRigid}
  \leanok
  \uses{def:isGenericallyRigid, lem:framework-finrank,
    def:isInfinitesimallyRigid}
  Let $V$ be finite and $G : \mathrm{SimpleGraph}\,V$. If $G$ is
  generically rigid in dimension $d$, there exists a framework
  $p : V \to \R^{d}$ with
  \[
    d \cdot |V| \;\le\; \dim_{\R}\,\bigl(\mathrm{range}\,(\mathrm{RigidityMap}\,G\,p)\bigr)
      + \frac{d(d+1)}{2}.
  \]
\end{lemma}
\begin{proof}
  \leanok
  Pick an infinitesimally rigid framework $p$ witnessing generic
  rigidity. Rank--nullity for $\mathrm{RigidityMap}\,G\,p$ gives
  $\dim\,\mathrm{Framework}\,V\,d = \dim\,\mathrm{range} + \dim\,\ker$;
  by \cref{lem:framework-finrank} the left-hand side equals $d \cdot |V|$,
  and by the definition of infinitesimal rigidity the nullity is at most
  $d(d+1)/2$. (The lemma is the rank-half of the edge-count theorem
  \cref{thm:isGenericallyRigid-card-mul-le} with the edge-count bound
  \cref{lem:rigidityMap-finrank-range-le} replaced by the rank itself.)
  The $(\Rightarrow)$ direction of Laman's theorem consumes this at
  $d = 2$, where $d(d+1)/2 = 3$ reduces by $\mathit{rfl}$ on the
  literal $\mathrm{Nat}$ arithmetic.
\end{proof}

\begin{lemma}[Row-independent edge basis from rank lower bound]
  \label{lem:exists-rowIndependent-edge-basis}
  \lean{SimpleGraph.exists_edgeSetRowIndependent_basis_dim_two}
  \leanok
  \uses{def:edgeSet-rowIndependent, def:rigidityRow,
    lem:rigidityMap-finrank-range-ge-of-isGenericallyRigid,
    lem:span-range-rigidityRow,
    lem:edgeSetRowIndependent-iff-linearIndepOn-rigidityRow}
  Let $V$ be finite. If $G$ is generically rigid in dimension $2$, then
  there exists a framework $p$ and an edge set $I \subseteq E(G)$ with
  $|I| = 2 |V| - 3$ which is row-independent at $p$.
\end{lemma}
\begin{proof}
  \leanok
  Pick $p$ realising the rank lower bound
  \cref{lem:rigidityMap-finrank-range-ge-of-isGenericallyRigid} at
  $d = 2$, so
  $\dim\,\mathrm{range}\,\mathrm{RigidityMap}\,G\,p \ge 2|V| - 3$. By
  \cref{lem:span-range-rigidityRow} the rigidity rows span the range
  of the dual map, which has the same dimension as
  $\mathrm{range}\,\mathrm{RigidityMap}\,G\,p$ by
  row-rank-equals-column-rank
  (\texttt{LinearMap.finrank\_range\_dualMap\_eq\_finrank\_range}).
  Extend $\emptyset$ to a linearly independent spanning subset of the
  row family via \texttt{exists\_linearIndepOn\_extension}; its
  cardinality equals the dimension and so is at least $2|V| - 3$.
  Truncate via \texttt{Set.exists\_subset\_card\_eq} and read $I$ as
  the edge labels indexing the chosen rows;
  \cref{lem:edgeSetRowIndependent-iff-linearIndepOn-rigidityRow}
  transports the dual-module linear independence back to
  \cref{def:edgeSet-rowIndependent}.
\end{proof}

\subsubsection{Rank upper bound under affine spanning}

The kernel of the rigidity map always contains the
\emph{trivial Euclidean motions} of the framework (translations plus
infinitesimal rotations); the unconditional $\le \ker$ inclusion and
the $d(d+1)/2$ lower bound on $\dim_{\R}\,\ker$ at an affinely-spanning
placement are developed in \cref{sec:trivial-motions} (specifically
\cref{lem:rigidityMap-ker-finrank-ge-of-affinelySpanning}). The
induced rank upper bound $\rk \le d \cdot |V| - d(d+1)/2$ is the
converse of the rank lower bound; specialised at $d = 2$ it underlies
the $(2, 3)$-sparsity claim below.

\begin{lemma}[Rank upper bound from affine spanning, d-general]
  \label{lem:rigidityMap-finrank-range-le-of-affinelySpanning}
  \lean{SimpleGraph.rigidityMap_finrank_range_le_of_affinelySpanning}
  \leanok
  \uses{lem:rigidityMap-ker-finrank-ge-of-affinelySpanning,
    lem:framework-finrank}
  Let $V$ be finite, let $G$ be a graph on $V$, and let
  $p : V \to \R^{d}$ affinely span. Then
  \[
    \dim_{\R}\,\bigl(\mathrm{range}\,(\mathrm{RigidityMap}\,G\,p)\bigr)
      + \frac{d(d+1)}{2} \;\le\; d \cdot |V|.
  \]
\end{lemma}
\begin{proof}
  \leanok
  Combine
  \cref{lem:rigidityMap-ker-finrank-ge-of-affinelySpanning} with
  rank--nullity and the framework-space dimension
  \cref{lem:framework-finrank}. The $(\Rightarrow)$ direction
  consumes this at $d = 2$ where $d(d+1)/2 = 3$ reduces by
  $\mathit{rfl}$.
\end{proof}

\subsubsection{Sparsity from row-independence (Lov\'asz--Yemini, easy direction)}

The sparsity claim transports the rank upper bound from the induced
subframework $H[S]$ at $p|_{S}$ back to the row count of the
$H[S]$-rows in $\mathrm{RigidityMap}\,G\,p$. The two sides match
exactly because edges in $H[S]$ have both endpoints in $S$, so the
corresponding rows are supported on the columns indexed by $S$.
We need the witness placement to affinely span on every
size-$\ge 3$ subset; \cref{lem:exists-affinelySpanning-rigid-placement}
below provides one at any dimension $d$ (consumed here at $d = 2$).

\begin{lemma}[Affinely-spanning rigid placement, dimension $d$]
  \label{lem:exists-affinelySpanning-rigid-placement}
  \lean{SimpleGraph.exists_affinelySpanning_rigid_placement}
  \leanok
  \uses{def:isGenericallyRigid, lem:isInfinitesimallyRigid-eventually}
  Let $V$ be finite and $G$ generically rigid in dimension $d$. There
  exists a framework $p : V \to \R^{d}$ which is infinitesimally rigid
  for $G$ and whose restriction $p|_{S}$ affinely spans $\R^{d}$ for
  every $S \subseteq V$ with $|S| \ge d + 1$.
\end{lemma}
\begin{proof}
  \leanok
  Pick an infinitesimally rigid witness $p_{0}$ from the
  generic-rigidity hypothesis. Choose an injection $\phi : V \to \R$
  via the finite-type bijection $V \simeq \mathrm{Fin}\,|V|$ composed
  with the natural injection $\mathrm{Fin}\,n \hookrightarrow \R$, and
  set the moment-curve direction $w(v) := (\phi(v), \phi(v)^{2},
  \ldots, \phi(v)^{d}) \in \R^{d}$. For $t \in \R$ define the
  perturbed placement $p_{t} := p_{0} + t \cdot w$.

  Openness of infinitesimal rigidity
  (\cref{lem:isInfinitesimallyRigid-eventually}) plus the continuity
  of $t \mapsto p_{t}$ supplies an $\varepsilon > 0$ such that $p_{t}$
  is infinitesimally rigid for every $|t| < \varepsilon$.

  For each ordered $(d+1)$-tuple $(v_{0}, \ldots, v_{d})$ of distinct
  vertices, the affine-dependence determinant of the perturbed
  difference matrix
  \[
    \det\bigl(p_{t}(v_{1}) - p_{t}(v_{0}),\, \ldots,\,
      p_{t}(v_{d}) - p_{t}(v_{0})\bigr)
  \]
  is a polynomial in $t$ of degree at most $d$
  (\texttt{Polynomial.natDegree\_det\_X\_add\_C\_le}) whose top
  coefficient (\texttt{Polynomial.coeff\_det\_X\_add\_C\_card}) is the
  Vandermonde-difference determinant
  $\prod_{0 \le i < j \le d} (\phi(v_{j}) - \phi(v_{i}))$
  (\texttt{Matrix.det\_powerDifferences}), nonzero by injectivity of
  $\phi$. The per-tuple bad-$t$ set is therefore finite
  (\texttt{Polynomial.finite\_setOf\_isRoot}). The global bad set is
  the finite union of finitely many finite sets, hence finite, so any
  point of the (uncountable) interval $(0, \varepsilon)$ outside the
  bad set is a valid choice of $t$. The resulting $p_{t}$ is
  infinitesimally rigid, and for every $S$ with $|S| \ge d + 1$ any
  $d + 1$ distinct elements of $S$ give an affinely-independent tuple
  under $p_{t}$, so $p_{t}|_{S}$ affinely spans $\R^{d}$.
\end{proof}

\begin{lemma}[$(2, 3)$-sparsity from row-independence, dim $2$]
  \label{lem:isSparse-of-rowIndependent-two}
  \lean{SimpleGraph.isSparse_of_edgeSetRowIndependent_dim_two}
  \leanok
  \uses{def:isSparse, def:edgeSet-rowIndependent,
    lem:rigidityMap-finrank-range-le-of-affinelySpanning,
    lem:exists-affinelySpanning-rigid-placement}
  Let $V$ be finite, $G : \mathrm{SimpleGraph}\,V$, and $p$ a framework
  on $V$ in dimension $2$ such that $p|_{S}$ affinely spans for every
  $S \subseteq V$ with $|S| \ge 3$. If $I \subseteq E(G)$ is
  row-independent at $p$, then the spanning subgraph $H \le G$ with
  edge set $I$ is $(2, 3)$-sparse.
\end{lemma}
\begin{proof}
  \leanok
  Fix $S \subseteq V$. The edges of $H$ with both endpoints in $S$
  are in $\mathrm{Sym}_{2}\,\iota$-bijection with the edges of the
  induced subgraph $H[S]$ via the canonical lift; we show
  $|\edgesIn[H]{S}| + 3 \le 2 |S|$.

  For $|S| \le 1$ the proviso $3 \le 2|S|$ in the definition of
  $(2, 3)$-sparsity is false, so the bound holds vacuously. For
  $|S| = 2$ the induced subgraph $H[S]$ has at most one edge (a simple
  graph on two vertices has at most one edge,
  \texttt{card\_edgeFinset\_le\_card\_choose\_two}), and
  $1 + 3 \le 4 = 2 |S|$.

  For $|S| \ge 3$: the rows of $\mathrm{RigidityMap}\,G\,p$ indexed
  by $\edgesIn[H]{S}$ are supported on the columns indexed by $S$
  (each row's nonzero entries are at the two endpoints of the
  corresponding edge, both of which lie in $S$). Hence row-independence
  of these rows in the full matrix transports through the
  framework-restriction map $\mathrm{Framework}\,V\,2 \to
  \mathrm{Framework}\,S\,2$ to row-independence of the
  $S$-indexed-column matrix, which is exactly the rigidity
  matrix $\mathrm{RigidityMap}\,(H[S])\,(p|_{S})$. The transport uses
  \texttt{LinearIndependent.of\_comp} on the dual-map side. By
  \cref{lem:rigidityMap-finrank-range-le-of-affinelySpanning} at
  $d = 2$ applied to $H[S]$ at the affinely-spanning placement
  $p|_{S}$, the rank of that matrix is at most $2 |S| - 3$. Since
  row-independence forces the row count $|\edgesIn[H]{S}|$ to equal
  the rank, $|\edgesIn[H]{S}| \le 2 |S| - 3$.
\end{proof}

\subsubsection{Assembly}

\begin{theorem}[Every generically rigid graph contains a Laman subgraph]
  \label{thm:isGenericallyRigid-exists-isLaman-le}
  \lean{SimpleGraph.IsGenericallyRigid.exists_isLaman_le}
  \leanok
  \uses{def:isGenericallyRigid, def:isLaman, def:isTight,
    lem:rigidityMap-finrank-range-ge-of-isGenericallyRigid,
    lem:exists-rowIndependent-edge-basis,
    lem:isSparse-of-rowIndependent-two,
    lem:exists-affinelySpanning-rigid-placement}
  Let $V$ be finite with $|V| \ge 2$. If $G$ is generically rigid in
  dimension $2$, then there exists a Laman spanning subgraph
  $H \le G$.
\end{theorem}
\begin{proof}
  \leanok
  Pick a framework $p$ that is both infinitesimally rigid for $G$
  and affinely-spanning on every size-$\ge 3$ subset, via
  \cref{lem:exists-affinelySpanning-rigid-placement}. At this $p$, the
  rank lower bound
  \cref{lem:rigidityMap-finrank-range-ge-of-isGenericallyRigid} (derived
  from infinitesimal rigidity at $p$ via rank--nullity) feeds the
  placement-fixed companion of \cref{lem:exists-rowIndependent-edge-basis}
  to extract $I \subseteq E(G)$ with $|I| = 2 |V| - 3$ row-independent
  at $p$. Let $H \le G$ be the spanning subgraph with edge set $I$. By
  \cref{lem:isSparse-of-rowIndependent-two}, $H$ is $(2, 3)$-sparse;
  together with $|E(H)| = 2 |V| - 3$ this makes $H$ $(2, 3)$-tight,
  i.e.\ Laman.
\end{proof}

\subsection{Laman's theorem}

\begin{theorem}[Laman's theorem]
  \label{thm:isGenericallyRigid-two-iff-exists-isLaman-le}
  \lean{SimpleGraph.isGenericallyRigid_two_iff_exists_isLaman_le}
  \leanok
  \uses{thm:isLaman-isGenericallyRigid-two,
    thm:isGenericallyRigid-exists-isLaman-le,
    lem:isGenericallyRigid-mono}
  Let $V$ be finite with $|V| \ge 2$ and $G : \mathrm{SimpleGraph}\,V$.
  Then $G$ is generically rigid in dimension $2$ iff $G$ contains a
  Laman spanning subgraph:
  \[
    G.\mathrm{IsGenericallyRigid}\,2
    \;\iff\;
    \exists H \le G,\; H.\mathrm{IsLaman}.
  \]
\end{theorem}
\begin{proof}
  \leanok
  The $\Rightarrow$ direction is
  \cref{thm:isGenericallyRigid-exists-isLaman-le}. For $\Leftarrow$:
  given $H \le G$ with $H$ Laman,
  \cref{thm:isLaman-isGenericallyRigid-two} gives
  $H.\mathrm{IsGenericallyRigid}\,2$, and monotonicity
  \cref{lem:isGenericallyRigid-mono} along $H \le G$ extends rigidity
  from $H$ to $G$.
\end{proof}
