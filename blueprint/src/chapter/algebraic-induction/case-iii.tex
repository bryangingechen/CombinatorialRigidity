\subsection{Case III deferred (the crux)}
\label{sec:molecular-algebraic-induction-caseIII}

The crux of Case III is the missing $+1$ row of KT Claim~6.11 (the $D$-th
$ab$-row of $R(G_v^{ab}, q)$ that lifts the stratum-1 brick
$D(|V|-1) - 1$ of \cref{lem:case-II-realization-placement} to full rank
$D(|V|-1)$). Claim~6.11's discharge factors, in dependency order, through three
combinatorial$\to$linear gaps; the leaf-most --- pure $M(\tilde G)$ matroid
theory, all inputs green --- is the matroid-base form of KT Lemma~4.3(ii) at
$k = 0$, built here.

\begin{lemma}[Matroid-base 4.3(ii) at $k = 0$; KT Lemma~4.3(ii), Claim~6.11 prerequisite]
  \label{lem:case-III-claim-6-11-base}
  \lean{Graph.splitOff_exists_base_inter_fiber_lt}
  \leanok
  \uses{def:k-dof, def:matroid-MG, thm:def-eq-corank, lem:forest-surgery-count, lem:splitoff-deficiency}
  Let $G$ be a $0$-dof-graph ($\operatorname{def}(\tilde G) = 0$) with a degree-2
  vertex $v$ (distinct neighbours $a, b$, incident edges exactly $e_a \ne e_b$,
  $e_0 \notin E(G)$ fresh). Then $M(\widetilde{G_v^{ab}})$ has a base $B'$ with
  \[
    \bigl|\widetilde{ab} \cap B'\bigr| \;<\; D - 1,
  \]
  i.e.\ $\widetilde{ab} \not\subseteq B'$: a redundant $\widetilde{ab}$-copy exists.
  This is KT's step~1 toward Claim~6.11 (the redundant $ab$-row of
  $R(G_v^{ab}, q)$); the remaining gaps --- the nested IH at the restricted
  realization and the $M(\tilde G)\leftrightarrow$row-dependence bridge --- stay
  deferred.
\end{lemma}
\begin{proof}
  \leanok
  Reroute a balanced $D$-forest packing of a base of $M(\tilde G)$ across $v$
  (\cref{lem:forest-surgery-count}): this yields an $M(\widetilde{G_v^{ab}})$-%
  independent set $I'$ with $|I'| = |\text{base}| - D$ and $|\widetilde{ab} \cap I'|
  < D - 1$ (KT Lemma~4.1's two conclusions). At $k = 0$ splitting off keeps the
  graph $0$-dof (\cref{lem:splitoff-deficiency}, with
  $\operatorname{def} \ge 0$), so $\operatorname{rank} M(\widetilde{G_v^{ab}}) =
  D(|V \setminus \{v\}| - 1) = |\text{base}| - D = |I'|$ by the def\,$=$\,corank
  identity (\cref{thm:def-eq-corank}); an independent set of full rank is a base.
  That base $I'$ carries the fiber bound.
\end{proof}

The second factor of Claim~6.11 is KT's nested-induction step: the
vertex-removal $G_v := G - v = G_v^{ab} - ab$ is itself a \emph{minimal}
$k'$-dof-graph for $k' := \operatorname{def}(\tilde G_v)$, and that deficiency is
small ($k' \le D - 2$). The combinatorial half --- minimality and the deficiency
bound, both pure $M(\tilde G)$ matroid theory --- is green here; the
\emph{analytic} half it feeds (applying the geometric IH to $G_v$ at the
inductively-fixed seed, KT eq.\ (6.22)) is the deferred crux.

\begin{lemma}[$G - v$ is minimal $k'$-dof with $k' \le D - 2$; KT Lemma~4.3(ii)/nested IH, Claim~6.11 prerequisite]
  \label{lem:case-III-gap3-minimalKDof}
  \lean{Graph.splitOff_removeVertex_minimalKDof}
  \leanok
  \uses{def:k-dof, def:graph-operations, lem:subgraph-minimality, thm:def-eq-corank,
        lem:splitoff-deficiency, lem:case-III-claim-6-11-base}
  Let $G$ be a minimal $0$-dof-graph with a degree-2 vertex $v$ (distinct
  neighbours $a, b$, incident edges exactly $e_a \ne e_b$, $e_0 \notin E(G)$
  fresh). Then the vertex-removal $G_v := G - v$ is a minimal
  $k'$-dof-graph for $k' = \operatorname{def}(\tilde G_v)$, with
  \[
    0 \;\le\; k' \;\le\; D - 2.
  \]
\end{lemma}
\begin{proof}
  \leanok
  Minimality transports along $G_v \le G$ by \cref{lem:subgraph-minimality}: a
  subgraph of a minimal $k$-dof-graph is minimal at its own deficiency. For the
  bound, take the Gap-2 base $B'$ of $M(\widetilde{G_v^{ab}})$ with
  $h := |\widetilde{ab} \cap B'| < D - 1$
  (\cref{lem:case-III-claim-6-11-base}). Its surviving part $B' \setminus
  \widetilde{ab}$ lands in $E(\tilde G_v)$ and is independent in $M(\tilde G_v) =
  M(\widetilde{G_v^{ab}}) \restriction E(\tilde G_v)$ (matroid restriction along
  $\tilde G_v \le \widetilde{G_v^{ab}}$), so $\operatorname{rank} M(\tilde G_v)
  \ge |B'| - h$. At $k = 0$ the splitting off is $0$-dof
  (\cref{lem:splitoff-deficiency}), so $|B'| = D(|V \setminus \{v\}| - 1)$; the
  def\,$=$\,corank identity (\cref{thm:def-eq-corank}, same vertex set $V(G)
  \setminus \{v\}$) then gives $\operatorname{def}(\tilde G_v) = D(|V \setminus
  \{v\}| - 1) - \operatorname{rank} M(\tilde G_v) \le h < D - 1$, i.e.\
  $k' \le D - 2$. The lower bound $0 \le k'$ is the nonnegativity of deficiency.
\end{proof}

The third factor is the \emph{analytic} kernel KT's footnote~6 supplies: the nested
induction fixes the realizing seed with algebraically-independent coordinates, so the
\emph{inductively-fixed} seed --- not merely \emph{some} generic seed --- attains the
maximal (matroid-predicted) rank of each subgraph. At the $0$-dof level this is KT
eq.~(6.18): the split-off $G_v^{ab}$ is $0$-dof, so its generic realization is rigid,
and the seed transfer below certifies the fixed seed is rigid for it too.

\begin{lemma}[Seed-rank bridge: rigidity transfers to every algebraically-independent seed; KT footnote 6, eqs.\ (6.18)/(6.22)]
  \label{lem:case-III-seed-rank-bridge}
  \lean{CombinatorialRigidity.Molecular.PanelHingeFramework.isInfinitesimallyRigidOn_ofNormals_of_algebraicIndependent}
  \leanok
  \uses{lem:genericity-device, def:rank-hypothesis}
  Let $\mathrm{ofNormals}\,G\,\mathit{ends}\,q_0$ be a free-normal panel framework with a
  link-recording selector and transversal hinges. If it is infinitesimally rigid on
  $V(G)$ at \emph{some} seed $q_0$, then it is infinitesimally rigid on $V(G)$ at
  \emph{any} seed $q$ that is algebraically independent over $\mathbb{Q}$.
\end{lemma}
\begin{proof}
  \leanok
  The rigid leg at $q_0$ carries a nonzero rank polynomial
  $Q : \mathrm{MvPolynomial}\,\sigma\,\mathbb{R}$ whose non-roots witness the full-size
  independent panel-row subfamily (the genericity device's coordinatization,
  \cref{lem:genericity-device}). $Q$'s coefficients are the structural change-of-basis
  constants of the complement isomorphism, hence rational
  ($Q.\mathrm{coeffs} \subseteq \mathrm{range}(\mathbb{Q} \to \mathbb{R})$). An
  algebraically-independent-over-$\mathbb{Q}$ seed $q$ lies off the zero locus of every
  nonzero rational polynomial (KT footnote~6), so $\mathrm{eval}\,q\,Q \ne 0$; the device
  consumer then reads infinitesimal rigidity at $q$ off that non-root.
\end{proof}

The seed-rank bridge above transfers \emph{full} rigidity (the $0$-dof case, eq.~(6.18)). The
nested IH step (eq.~(6.22)) needs the $\operatorname{def} > 0$ counterpart: at the same
inductively-fixed seed, the nested subgraph $G_v$ (minimal $k'$-dof, $k' = \operatorname{def}(\tilde
G_v)$) attains rank $D(|V_v|-1) - k'$, equivalently $\dim Z(G_v, q) = D + k'$. Pairing the
genericity-free \emph{lower} bound $D + \operatorname{def} \le \dim Z$ (the $\mathit{hub}$ input of
the Proposition-1.1 reconciliation, \cref{prop:rigidity-matrix-prop11}) with the matching
\emph{upper} bound below pins the equality. The upper bound is the brick green here.

\begin{lemma}[Eq.~(6.22) rank upper bound at every algebraically-independent seed; KT footnote 6, eq.\ (6.22)]
  \label{lem:case-III-seed-rank-upper}
  \lean{CombinatorialRigidity.Molecular.PanelHingeFramework.finrank_infinitesimalMotions_le_of_rankPolynomial_algebraicIndependent}
  \leanok
  \uses{lem:case-III-seed-rank-bridge, thm:def-eq-corank}
  Let $\mathrm{ofNormals}\,G\,\mathit{ends}\,\cdot$ be the free-normal panel framework, and let
  $Q$ be a nonzero \emph{rational} polynomial (coefficients in the image of $\mathbb{Q} \to
  \mathbb{R}$) whose non-roots witness a linearly independent $\mathrm{panelRow}$-subfamily $s$
  whose edges link in $G$. Then at \emph{any} seed $q$ algebraically independent over $\mathbb{Q}$,
  \[
    \dim Z(G, q) \;\le\; D\,|\alpha| - |s|.
  \]
\end{lemma}
\begin{proof}
  \leanok
  An algebraically-independent-over-$\mathbb{Q}$ seed is a non-root of the nonzero rational $Q$
  (KT footnote~6, as in \cref{lem:case-III-seed-rank-bridge}), so the witnessed subfamily $s$ is
  linearly independent at $q$ itself. Each panel row of $s$ lies in the rigidity rows (its edge
  links in $G$), so $|s| \le \dim \operatorname{span}(\text{rigidity rows}) = D\,|\alpha| - \dim
  Z(G, q)$ by rank--nullity (the rigidity-row span and the null space are dual-annihilator
  complements). Rearranging gives the bound.
\end{proof}

The upper bound above is the last \emph{leaf}; the two analytic halves now assemble. The nested
subgraph $G_v$ attains its predicted rank at the inductively-fixed seed by pinning the equality
between the genericity-free lower bound and the matroid-predicted upper bound.

\begin{lemma}[Eq.~(6.22) rank attainment at the inductively-fixed seed; KT footnote 6, eq.\ (6.22)]
  \label{lem:case-III-rank-attainment}
  \lean{CombinatorialRigidity.Molecular.PanelHingeFramework.rankHypothesis_ofNormals_of_rankPolynomial_algebraicIndependent}
  \leanok
  \uses{lem:case-III-seed-rank-upper, lem:trivial-motions-rank-bound, thm:def-eq-corank}
  Let $\mathrm{ofNormals}\,G\,\mathit{ends}\,q$ be the free-normal panel framework on the
  spanning graph $G$ ($V(G) = \alpha$) with every hinge genuine ($C(e) \neq 0$), and fix the
  inductively-fixed seed $q$, algebraically independent over $\mathbb{Q}$. Suppose a nonzero
  \emph{rational} polynomial $Q$ witnesses a linearly independent $\mathrm{panelRow}$-subfamily $s$
  (edges linking in $G$) of the matroid-predicted full size $|s| \ge D(|V|-1) -
  \operatorname{def}(\tilde G)$. Then $G$ attains its predicted rank at $q$:
  \[
    \dim Z(G, q) \;=\; D + \operatorname{def}(\tilde G),
    \qquad\text{i.e.}\qquad F.\,\mathrm{RankHypothesis}\bigl(\operatorname{def}(\tilde G)\bigr).
  \]
\end{lemma}
\begin{proof}
  \leanok
  The eq.~(6.22) upper bound (\cref{lem:case-III-seed-rank-upper}) gives $\dim Z(G, q) \le D|V| -
  |s|$ at $q$. Substituting the matroid-predicted size $|s| \ge D(|V|-1) - \operatorname{def}$
  (the matroid rank $\operatorname{rank} M(\tilde G) = D(|V|-1) - \operatorname{def}$ of the
  def\,$=$\,corank identity \cref{thm:def-eq-corank}) and the spanning split $D|V| = D(|V|-1) + D$
  collapses the upper bound to $\dim Z(G, q) \le D + \operatorname{def}$. The matching
  genericity-free lower bound $D + \operatorname{def} \le \dim Z(G, q)$ --- the $\mathit{hub}$ bound
  of \cref{lem:trivial-motions-rank-bound}, available since every hinge is genuine --- pins the
  equality. Deficiency nonnegativity rules out the degenerate over-count.
\end{proof}

The two rank attainments above (eq.~(6.18) for $G_v^{ab}$, eq.~(6.22) for $G_v = G_v^{ab} - ab$)
combine into KT's eq.~(6.23): the $D-1$ many $ab$-rows of $R(G_v^{ab}, q)$ raise the rank of
$R(G_v, q)$ by only $k' \le D - 2 < D - 1$ (the smaller graph $G_v$ already realizes all but $k'$ of
the full rank), so one of those $ab$-rows is a linear combination of the others together with the
$R(G_v, q)$ rows. The pure linear-algebra core of that pigeonhole
(\cref{lem:case-III-claim-6-11-redundant-row}) is instantiated at KT's two specific graphs by the
\emph{row-set identity} $\operatorname{span}(R(G_v^{ab})\text{-rows}) =
\operatorname{span}(R(G_v)\text{-rows}) + \operatorname{span}(ab\text{-block})$: since
$G_v = G_v^{ab} - ab$ and both frameworks share the same seed $q$ and selector $\mathrm{ends}$ (so
the same supporting extensors), the only rigidity rows of $R(G_v^{ab})$ not already in $R(G_v)$ are
the $ab$-rows, which span exactly the $ab$-block. Feeding the two rank attainments through it
discharges the corank gap and produces KT's redundant $ab$-row
(\cref{lem:case-III-claim-6-11}).

\begin{lemma}[Redundant $ab$-row from a small corank over the $ab$-block; KT Claim 6.11, eq.\ (6.23)]
  \label{lem:case-III-claim-6-11-redundant-row}
  \lean{CombinatorialRigidity.Molecular.BodyHingeFramework.exists_redundant_panelRow_of_edge_of_finrank_lt}
  \leanok
  \uses{lem:case-III-seed-rank-bridge, lem:case-III-rank-attainment,
        lem:case-III-gap3-minimalKDof, lem:case-II-placement-new-rows}
  Let $F$ be a body-hinge framework with a transversal hinge $e$ (distinct endpoints, nonzero
  supporting extensor $C(e) \ne 0$), and let $W$ be any subspace of the dual row space (intended:
  $W = \operatorname{span}(R(G_v, q)\text{-rows})$ for the smaller graph $G_v = G_v^{ab} - ab$, with
  $e = ab$). If the $e$-block --- the per-edge panel-row span
  $\operatorname{span}\{\mathrm{panelRow}\,(e, \cdot, \cdot)\}$, of dimension $D - 1$ --- raises the
  finrank of $W$ by strictly fewer than $D - 1$,
  \[
    \dim\bigl(W + \operatorname{span}(e\text{-block})\bigr) \;<\; \dim W + (D - 1),
  \]
  then there is a $\mathrm{Fin}(D-1)$-indexed independent family $r$ of the $e$-block (its span is
  the whole block) with one redundant member: $r\,i_0 \in W + \operatorname{span}(r \restriction
  \{j \ne i_0\})$. This is KT's eq.~(6.23): with the corank gap fed by eq.~(6.18)
  (\cref{lem:case-III-seed-rank-bridge}, full rank of $R(G_v^{ab}, q)$ at the $0$-dof $G_v^{ab}$) and
  eq.~(6.22) (\cref{lem:case-III-rank-attainment}, with $k' = \operatorname{def}(\tilde G_v) \le
  D - 2$ from \cref{lem:case-III-gap3-minimalKDof}), one of the $ab$-rows is redundant, so dropping
  it does not lower the rank.
\end{lemma}
\begin{proof}
  \leanok
  The $e$-block is the $\mathrm{hingeRow}$ image of the $(D-1)$-dimensional hinge-row block
  $r(p(e))$, so it is $(D-1)$-dimensional and is spanned by the $D - 1$ independent panel rows the
  transversal hinge contributes (\cref{lem:case-II-placement-new-rows}). The finrank hypothesis is
  exactly the abstract pigeonhole's input at this $\mathrm{Fin}(D-1)$-indexed family: a family
  raising $\dim W$ by fewer than its cardinality has a member lying in $W$ plus the span of the rest.
  That member is the redundant $ab$-row.
\end{proof}

\begin{lemma}[KT Claim 6.11, eq.\ (6.23): the deleted $ab$-edge has a redundant row]
  \label{lem:case-III-claim-6-11}
  \lean{CombinatorialRigidity.Molecular.BodyHingeFramework.exists_redundant_panelRow_ab_of_finrank_eq}
  \leanok
  \uses{lem:case-III-claim-6-11-redundant-row, lem:case-III-seed-rank-bridge,
        lem:case-III-rank-attainment, lem:case-III-gap3-minimalKDof}
  Let $G_{ab} = G_v^{ab}$ be the split-off graph and $G_v = G_v^{ab} - ab$ its single-edge deletion,
  realized as $\mathrm{ofNormals}\,(\cdot)\,\mathrm{ends}\,q$ at the same inductively-fixed seed $q$
  and selector $\mathrm{ends}$, with $e_0 = ab$ the deleted transversal hinge ($C(e_0) \ne 0$). Given
  the two rank attainments --- eq.~(6.18) $\dim \operatorname{span}(R(G_{ab}, q)\text{-rows}) =
  D(m-1)$ (the $0$-dof full rank, $m = |V(G_{ab})| = |V(G_v)|$) and eq.~(6.22)
  $\dim \operatorname{span}(R(G_v, q)\text{-rows}) = D(m-1) - k'$ with $k' \le D - 2$ --- one of the
  $D-1$ many $ab$-rows of $R(G_{ab}, q)$ is redundant modulo $\operatorname{span}(R(G_v, q)\text{-rows})$
  and the other $ab$-rows: KT's eq.~(6.23). This is the $+1$ that the deferred candidate-completion
  assembly converts into the missing full-rank row.
\end{lemma}
\begin{proof}
  \leanok
  The two frameworks differ only by the $ab$-edge $e_0$ (their links agree off $e_0$, and they share
  the same supporting extensors), so the row-set identity $\operatorname{span}(R(G_{ab})\text{-rows})
  = \operatorname{span}(R(G_v)\text{-rows}) + \operatorname{span}(ab\text{-block})$ holds. Eq.~(6.18)
  then reads $\dim\bigl(W + \operatorname{span}(ab\text{-block})\bigr) = D(m-1)$ with
  $W = \operatorname{span}(R(G_v)\text{-rows})$, while eq.~(6.22) gives $\dim W = D(m-1) - k'$; so the
  $ab$-block raises $\dim W$ by only $k' \le D - 2 < D - 1$, the corank gap the redundant-row
  pigeonhole (\cref{lem:case-III-claim-6-11-redundant-row}) consumes to yield the redundant
  $ab$-row.
\end{proof}

\subsubsection{The candidate-completion: converting the redundant row into the
missing \texorpdfstring{$+1$}{+1} (KT eqs.\ (6.24)--(6.29))}
\label{sec:molecular-algebraic-induction-candidate-completion}

Claim~6.11 (\cref{lem:case-III-claim-6-11}) gives a redundant $ab$-row of
$R(G_v^{ab}, q)$; the candidate-completion turns it into the full-rank $D$-th row
of the stratum-1 brick. KT's mechanism (eqs.~(6.24)--(6.29)) is a single fundamental
row operation \emph{followed by the eqs.~(6.14)--(6.15) column operation}: the redundant
row's linear combination, transported from $R(G_v^{ab}, q)$ up to $R(G, p_1)$ and then
column-reduced (add column $v$ to column $a$), produces a row $w$ of $R(G, p_1)$ whose
$V \setminus \{v\}$ part vanishes (eqs.~(6.27)--(6.28)). The eqs.~(6.24)--(6.25) decomposition
of the redundant row (\cref{lem:case-III-redundant-decomposition}, green), the per-edge seam
identity (\cref{lem:case-III-seam}, green), and the pure-$v$-column characterization of such a
row (\cref{lem:case-III-vanish-off-column}, green) are in place, as is the column operation $\Phi$
itself: a Phase~22e recon found it load-bearing for the vanishing --- the transported row
$w = \mathrm{hingeRow}\ v\ a\ \rho_g$ is supported on \emph{both} columns $v$ and $a$, so the column
op, not the seam alone, is what makes its $V \setminus \{v\}$ part zero --- and
\cref{lem:case-III-columnop} now supplies $\Phi$ (the $\mathrm{col}_a \mathrel{+}= \mathrm{col}_v$
change-of-variables automorphism) together with the fact that $w \circ \Phi$ is pure $v$-column.
The per-row eq.~(6.27) transport collapse (\cref{lem:case-III-transport-collapse}, green) closes the
core algebra: each transported $(vb)j$-row minus the inductive $(ab)j$-row is the pure $(va)$-hinge
row $\mathrm{hingeRow}\ v\ a\ (\mathrm{annihRow}\ C\ \cdot)$ (same supporting extensor by the shear
identity), and it packages the column-op composition certifying $w \circ \Phi$ pure $v$-column.
The combination-level threading is itself now green
(\cref{lem:case-III-candidate-row-construction}): the redundant row's common $G_v$-row element
$w_{\mathrm{Gv}}$ is an $ab$-block element, so it is $\mathrm{hingeRow}\ a\ b\ \rho$ for some
$\rho \in r(p(e_0))$, its transported $(vb)i^*$-row $\mathrm{hingeRow}\ v\ b\ \rho$ is a genuine
rigidity row of $R(G, p_1)$, and the collapse yields the candidate row $\mathrm{hingeRow}\ v\ a\ \rho$.
The full $D(|V|-1)$-family assembly is now in place (\cref{lem:case-III-candidate-row}, green): the
operated row $w \circ \Phi$ joins the stratum-1 brick's $va$-block and old block in the pin-a-body
augment (\cref{lem:case-III-conditional-block}), and the operated independence transports back
through the automorphism $\Phi$ to the original family of size $((D-1)+1)+D(|V_v|-1) = D(|V|-1)$.
What remains is the one \emph{conditional} it passes through --- the eq.~(6.29) top-left $D \times D$
full rank (\cref{lem:case-III-eq629-conditional}) --- which Claim~6.12 will discharge. The
$a$-column-block vanishing (\cref{lem:case-III-acolumn-zero}, green) is a Claim~6.12 fact (the $M_3$
case), not the candidate-row input.

\begin{lemma}[A rigidity row vanishing off $v$'s column is a pure $v$-column row; KT eq.\ (6.28)]
  \label{lem:case-III-vanish-off-column}
  \lean{CombinatorialRigidity.Molecular.BodyHingeFramework.dualMap_eq_comp_single_proj_of_vanish_off}
  \leanok
  Let $f$ be a functional on the screw assignments $\alpha \to \mathrm{ScrewSpace}\,k$
  that vanishes on every assignment supported off a fixed body $v$ (i.e.\
  $S v = 0 \Rightarrow f S = 0$). Then $f$ factors through body $v$'s screw column:
  $f = (f \circ \mathrm{single}_v) \circ \mathrm{proj}_v$. This is KT eq.~(6.28)'s
  formal content: the candidate-completion's row operation (eqs.~(6.24)--(6.27))
  reduces the $(vb)i^*$-row of $R(G, p_1)$ to a row whose $V \setminus \{v\}$ part is
  all zero, and such a row is determined by its $v$-column block --- written
  $S = \mathrm{single}_v(S v) + (S - \mathrm{single}_v(S v))$, the second summand is
  supported off $v$, so $f$ kills it.
\end{lemma}
\begin{proof}
  \leanok
  Split $S = \mathrm{single}_v(S v) + (S - \mathrm{single}_v(S v))$. The second
  summand reads $0$ at $v$, so $f$ kills it by hypothesis, leaving
  $f S = f(\mathrm{single}_v(S v)) = (f \circ \mathrm{single}_v)(S v)$, which is
  exactly $((f \circ \mathrm{single}_v) \circ \mathrm{proj}_v)\,S$.
\end{proof}

\begin{lemma}[The column operation $\Phi$: the candidate row is pure $v$-column in the operated frame; KT eqs.\ (6.14)--(6.16)]
  \label{lem:case-III-columnop}
  \lean{CombinatorialRigidity.Molecular.BodyHingeFramework.columnOp,
        CombinatorialRigidity.Molecular.BodyHingeFramework.hingeRow_comp_columnOp_apply,
        CombinatorialRigidity.Molecular.BodyHingeFramework.hingeRow_comp_columnOp_vanish_off}
  \leanok
  \uses{lem:case-III-vanish-off-column}
  KT's column operation $\mathrm{col}_a \mathrel{+}= \mathrm{col}_v$ (eqs.~(6.14)--(6.15)), the
  device that makes the transported candidate row vanish off $v$. Since the rigidity rows read
  relative screws $S_u - S_w$, the operation is modelled by its dual substitution on the screw
  assignment $\alpha \to \mathrm{ScrewSpace}\,k$:
  $\Phi(S) = \mathrm{update}\ S\ v\ (S_v + S_a)$, a linear automorphism for $v \neq a$ (inverse
  $\Phi^{-1}(S) = \mathrm{update}\ S\ v\ (S_v - S_a)$). It is rank-preserving (precomposition with a
  linear equivalence), so it certifies the candidate row's vanishing without changing any rank. The
  transported candidate row $w = \mathrm{hingeRow}\ v\ a\ \rho$ is supported on \emph{both} columns
  $v$ and $a$, with $w(S) = \rho(S_v - S_a)$; precomposing with $\Phi$ reads $\Phi(S)$ as $S_v + S_a$
  at $v$ and $S_a$ at $a$ (using $v \neq a$), so
  \[ w(\Phi(S)) = \rho\bigl((S_v + S_a) - S_a\bigr) = \rho(S_v), \]
  the $a$-column contribution cancels. In particular $w(\Phi(S)) = 0$ whenever $S_v = 0$, which is
  exactly the off-$v$ vanishing hypothesis \cref{lem:case-III-vanish-off-column} consumes: $w \circ
  \Phi$ is a pure $v$-column row. This --- KT's eqs.~(6.14)--(6.16) column reduction --- is the
  vanishing mechanism, \emph{not} the per-edge seam plus eq.~(6.43).
\end{lemma}
\begin{proof}
  \leanok
  $\Phi$ is a linear equivalence by direct verification of additivity, homogeneity, and the two
  inverse identities (each a case split on whether the coordinate is $v$; the off-$v$ coordinates are
  untouched, the $v$-coordinate cancels by $(S_v + S_a) - S_a = S_v$ and $(S_v - S_a) + S_a = S_v$,
  using $v \neq a$ so $\Phi(S)_a = S_a$). The evaluation $w(\Phi(S)) = \rho(S_v)$ is then immediate
  from $\Phi(S)_v = S_v + S_a$, $\Phi(S)_a = S_a$, and the cancellation; the $S_v = 0$ case
  substitutes $\rho(0) = 0$.
\end{proof}

\begin{lemma}[The seam identity: an edge's row depends only on the seed at its endpoints; KT eq.\ (6.26)]
  \label{lem:case-III-seam}
  \lean{CombinatorialRigidity.Molecular.PanelHingeFramework.ofNormals_panelRow_eq_of_ends_seed_eq}
  \leanok
  Two $\mathrm{ofNormals}$ realizations at seeds $q_0$, $q$ that \emph{agree at the
  two endpoints} of an edge $e$ --- $q_0(\mathrm{ends}\,e)_1 = q(\mathrm{ends}\,e)_1$
  and $q_0(\mathrm{ends}\,e)_2 = q(\mathrm{ends}\,e)_2$ as panel normals --- produce
  identical $e$-rows of their rigidity matrices: for every basis pair $(t_1, t_2)$,
  \[ R(G, q_0; e, t_1, t_2) = R(G_v^{ab}, q; e, t_1, t_2), \]
  regardless of the two underlying graphs. This is KT's seam identity
  $R(G, p_1; E \setminus \{vb\}, V \setminus \{v\}) = R(G_v^{ab}, q)$ in per-row form:
  a panel row $\mathrm{hingeRow}\,(\mathrm{ends}\,e)_1\,(\mathrm{ends}\,e)_2\,
  (\mathrm{annihRow}\,C(p(e))\,t_1\,t_2)$ reads only the endpoint selector, the index,
  and the supporting extensor $C(p(e)) = \mathrm{panelSupportExtensor}\,(\mathrm{normal}\,
  (\mathrm{ends}\,e)_1)\,(\mathrm{normal}\,(\mathrm{ends}\,e)_2)$, which depends on the
  seed only at the two endpoints (the underlying graph never enters a panel row).
\end{lemma}
\begin{proof}
  \leanok
  Both rows unfold to $\mathrm{hingeRow}\,(\mathrm{ends}\,e)_1\,(\mathrm{ends}\,e)_2\,
  (\mathrm{annihRow}\,C\,t_1\,t_2)$ with $C = \mathrm{panelSupportExtensor}$ of the two
  endpoint normals; the hypotheses $q_0 = q$ at $(\mathrm{ends}\,e)_1$, $(\mathrm{ends}
  \,e)_2$ make the two normals equal, hence the extensors and the rows.
\end{proof}

\begin{lemma}[The redundant $ab$-row as an explicit vanishing combination; KT eqs.\ (6.24)--(6.25)]
  \label{lem:case-III-redundant-decomposition}
  \lean{CombinatorialRigidity.Molecular.BodyHingeFramework.exists_redundant_panelRow_ab_decomposition}
  \leanok
  \uses{lem:case-III-claim-6-11}
  The functional-identity form of Claim~6.11 (\cref{lem:case-III-claim-6-11}, eq.~(6.23)) that the
  candidate-completion row operation consumes. Where \cref{lem:case-III-claim-6-11} delivers the
  \emph{membership} $r\,i^* \in \operatorname{span}(R(G_v, q)\text{-rows}) \sqcup
  \operatorname{span}(r \mathbin{''} \{j \neq i^*\})$ --- the $D - 1$ independent $ab$-rows $r$
  spanning the $ab$-block, one of them ($i^*$) redundant modulo the $G_v$-rows and the rest --- this
  lemma unwinds it (submodule $\sqcup$-membership) into KT's eq.~(6.24): the redundant $ab$-row
  $r\,i^*$ \emph{equals} a $G_v$-row element $w_{\mathrm{Gv}} \in \operatorname{span}(R(G_v,
  q)\text{-rows})$ plus a combination $w_{\mathrm{Other}} \in \operatorname{span}(r \mathbin{''} \{j
  \neq i^*\})$ of the other $ab$-rows. Rearranged this is the vanishing combination
  $r\,i^* - w_{\mathrm{Other}} - w_{\mathrm{Gv}} = 0$ of eq.~(6.24) with the $(ab)i^*$-coefficient
  $\lambda_{(ab)i^*} = 1$ (eq.~(6.25)): $r\,i^*$ carries coefficient $1$, $w_{\mathrm{Other}}$ the
  other $\lambda_{(ab)j}$ ($j \neq i^*$), $w_{\mathrm{Gv}}$ the $\lambda_{ej}$ over $E_v$.
\end{lemma}
\begin{proof}
  \leanok
  Immediate from \cref{lem:case-III-claim-6-11} by splitting the $\sqcup$-membership of the redundant
  row $r\,i^*$ into its $\operatorname{span}(R(G_v, q)\text{-rows})$ summand $w_{\mathrm{Gv}}$ and its
  $\operatorname{span}(r \mathbin{''} \{j \neq i^*\})$ summand $w_{\mathrm{Other}}$.
\end{proof}

\begin{lemma}[The $a$-column block of the eq.\ (6.24) vanishing combination is $0$; KT eq.\ (6.43)]
  \label{lem:case-III-acolumn-zero}
  \lean{CombinatorialRigidity.Molecular.BodyHingeFramework.exists_redundant_panelRow_ab_decomposition_acolumn_zero}
  \leanok
  \uses{lem:case-III-redundant-decomposition}
  The eqs.~(6.24)--(6.25) decomposition (\cref{lem:case-III-redundant-decomposition}) writes the
  redundant $ab$-row as $r\,i^* = w_{\mathrm{Gv}} + w_{\mathrm{Other}}$, i.e.\ the vanishing
  combination $g := w_{\mathrm{Gv}} + w_{\mathrm{Other}} - r\,i^* = 0$ --- KT's eq.~(6.24)
  $\sum_j \lambda_{(ab)j} R(G_v^{ab}, q; (ab)j) + \sum_{e \in E_v, j} \lambda_{ej} R(G_v^{ab}, q;
  e_j) = 0$ as a functional on the screw assignments $\alpha \to \mathrm{ScrewSpace}\,k$. KT
  eq.~(6.43) is its restriction to any single body $a$'s screw column: precomposing $g$ with the
  column injection $\mathrm{single}_a \colon \mathrm{ScrewSpace}\,k \to (\alpha \to
  \mathrm{ScrewSpace}\,k)$ (a screw on $a$, $0$ elsewhere) is again $0$,
  \[ g \circ \mathrm{single}_a = 0, \qquad\text{i.e.}\qquad
     \sum_{e \in E_v \cup \{ab\},\, j} \lambda_{ej}\, R(G_v^{ab}, q; e_j, a) = 0. \]
  This is the residual that the candidate-completion transport
  (\cref{lem:case-III-candidate-row}) must kill to certify that the transported row $w$'s
  $V \setminus \{v\}$ part vanishes: over $V \setminus \{v\}$ the $(vb)j$-row of $R(G, p_1)$ and the
  $(ab)j$-row of $R(G_v^{ab}, q)$ differ by exactly the $a$-column block $r_j(\cdot\,a)$, so $w$'s
  $V \setminus \{v\}$ part is the eq.~(6.24) sum ($= g(S) = 0$) minus that $a$-block.
\end{lemma}
\begin{proof}
  \leanok
  Since $r\,i^* = w_{\mathrm{Gv}} + w_{\mathrm{Other}}$
  (\cref{lem:case-III-redundant-decomposition}), the combination $g = w_{\mathrm{Gv}} +
  w_{\mathrm{Other}} - r\,i^*$ is the zero functional; precomposing it with $\mathrm{single}_a$ is
  therefore $0$ for every body $a$.
\end{proof}

\begin{lemma}[The conditional $D$-row block: the operated row lifts the $va$-block from $D-1$ to $D$; KT eq.\ (6.29)]
  \label{lem:case-III-conditional-block}
  \lean{CombinatorialRigidity.Molecular.BodyHingeFramework.linearIndependent_sum_pinned_block_augment}
  \leanok
  \uses{lem:case-II-placement-block-independent, lem:case-III-vanish-off-column,
        lem:case-III-columnop}
  KT eq.~(6.29)'s block-triangular $+1$, the assembly that takes the stratum-1 brick
  $D(|V|-1) - 1$ (\cref{lem:case-II-realization-placement}) to full $D(|V|-1)$ \emph{conditional} on
  the top-left $D \times D$ block being full rank. The new block is the $D - 1$ rows $r_n$ of body
  $v$'s $va$-hinge \emph{plus} the candidate-completion's operated extra row $w$ --- the pure
  $v$-column row $\mathrm{hingeRow}\,v\,a\,\rho_g \circ \Phi$ produced by the column op
  (\cref{lem:case-III-columnop}, \cref{lem:case-III-vanish-off-column}); the old block is the
  $D(|V_v|-1)$ rows $r_o$ of the split-off $G_v^{ab} \setminus (ab)i^*$, vanishing at
  $\mathrm{update}\ 0\ v$. The two blocks are jointly independent ---
  $\mathrm{Sum.elim}\,(\mathrm{Sum.elim}\,r_n\,(\lambda \_.\ w))\,r_o$ --- provided the
  \emph{augmented} pinned new block $\mathrm{Sum.elim}\,(r_n \circ \mathrm{single}_v)\,(w \circ
  \mathrm{single}_v)$ of $D$ functionals is independent on body $v$'s $D$-dimensional screw column
  (KT's eq.~(6.29) top-left $D \times D$ full rank: the $va$-block's $D-1$ pinned rows plus the
  operated $(vb)i^*$-row $w$'s $v$-column block). The $+1$ over the stratum-1 brick is the extra
  row; the count becomes $((D-1)+1) + D(|V_v|-1) = D(|V|-1)$.
\end{lemma}
\begin{proof}
  \leanok
  \uses{lem:case-II-placement-block-independent}
  $w$ is a new-block row pinned through $v$'s column (the pure-$v$-column property of the operated
  row, carried into the augmented-pinned-independence hypothesis), so it joins $r_n$ in the
  pin-a-body new block rather than needing the old-block vanishing. The statement is then exactly
  the pin-a-body column split (\cref{lem:case-II-placement-block-independent}) applied to the
  augmented new block indexed by $\iota_n \oplus \{\ast\}$: the augmented new block composed with
  $\mathrm{single}_v$ equals the supplied augmented pinned family (the $\mathrm{Sum.elim}$
  distributes over the per-index $\circ\,\mathrm{single}_v$), discharging $h_{\mathrm{newpin}}$.
\end{proof}

\begin{lemma}[The eq.\ (6.27) transport collapse: the $(vb)$-row minus the $(ab)$-row is a $(va)$-hinge row; KT eqs.\ (6.26)--(6.27)]
  \label{lem:case-III-transport-collapse}
  \lean{CombinatorialRigidity.Molecular.PanelHingeFramework.panelRow_vb_sub_panelRow_ab_eq_hingeRow_va,
        CombinatorialRigidity.Molecular.BodyHingeFramework.hingeRow_sub_hingeRow_eq,
        CombinatorialRigidity.Molecular.BodyHingeFramework.comp_columnOp_eq_comp_single_proj}
  \leanok
  \uses{lem:case-III-seam, lem:case-III-columnop, lem:case-III-vanish-off-column}
  The per-row form of KT eq.~(6.27)'s transport step, plus the column-op packaging that closes the
  eq.~(6.28) vanishing. At the degenerate eq.~(6.12) placement $p_1 = q_0$ ($v$'s normal at
  $n_a + t\,n_b$, $q_0 = q$ at the surviving endpoint $b$), the transported $(vb)j$-row of
  $R(G, p_1)$ and the inductive $(ab)j$-row of $R(G_v^{ab}, q)$ read the \emph{same} supporting
  extensor $C = \mathrm{panelSupportExtensor}\,n_a\,n_b$ --- the shear identity
  $\mathrm{panelSupportExtensor}\,(n_a + t\,n_b)\,n_b = \mathrm{panelSupportExtensor}\,n_a\,n_b$
  (KT eq.~(6.16)) --- so they are $\mathrm{hingeRow}\,v\,b\,(\mathrm{annihRow}\,C\,t_1\,t_2)$ and
  $\mathrm{hingeRow}\,a\,b\,(\mathrm{annihRow}\,C\,t_1\,t_2)$, and their difference is the pure
  $(va)$-hinge row
  \[ R(G, p_1; (vb)j) - R(G_v^{ab}, q; (ab)j) = \mathrm{hingeRow}\,v\,a\,(\mathrm{annihRow}\,C\,t_1\,t_2), \]
  by the hinge-difference collapse $\mathrm{hingeRow}\,v\,b\,\rho - \mathrm{hingeRow}\,a\,b\,\rho =
  \mathrm{hingeRow}\,v\,a\,\rho$ (since $(S_v - S_b) - (S_a - S_b) = S_v - S_a$). This is exactly the
  collapse KT eq.~(6.27) performs: transporting the redundant-$(ab)$-row combination as $(vb)$-rows
  and subtracting the inductive $(ab)$-combination (which the eq.~(6.24) decomposition makes vanish,
  $r\,i^* = w_{\mathrm{Gv}} + w_{\mathrm{Other}}$) collapses the transported row to
  $w = \mathrm{hingeRow}\,v\,a\,\rho_g$. The companion fact composes the column op $\Phi$
  (\cref{lem:case-III-columnop}) with the off-$v$ vanishing (\cref{lem:case-III-vanish-off-column}):
  the operated row $w \circ \Phi$ \emph{is} its own pure $v$-column factorization
  $(w \circ \Phi \circ \mathrm{single}_v) \circ \mathrm{proj}_v$, the eq.~(6.29) $(vb)i^*$-row.
\end{lemma}
\begin{proof}
  \leanok
  Both panel rows unfold to $\mathrm{hingeRow}\,\cdot\,b\,(\mathrm{annihRow}\,C\,t_1\,t_2)$ with the
  same $C$ (the shear identity at $q_0$ vs.\ the $(ab)$-extensor at $q$); the hinge-difference
  identity then gives the $(va)$-hinge row. The column-op packaging is the composition of
  \cref{lem:case-III-columnop} (the operated row vanishes off $v$) with
  \cref{lem:case-III-vanish-off-column} (a row vanishing off $v$ factors through $v$'s column).
\end{proof}

\begin{lemma}[The candidate row $w$ from the redundant row's common element; KT eqs.\ (6.24)--(6.28)]
  \label{lem:case-III-candidate-row-construction}
  \lean{CombinatorialRigidity.Molecular.PanelHingeFramework.exists_candidate_row_eq612}
  \leanok
  \uses{lem:case-III-redundant-decomposition, lem:case-III-transport-collapse}
  The combination-level threading that produces the candidate row $w$ from KT's redundant-$ab$-row.
  The input is the \emph{common} element $w_{\mathrm{Gv}}$ of the eqs.~(6.24)--(6.25) decomposition
  (\cref{lem:case-III-redundant-decomposition}): the $G_v$-row part of the redundant $ab$-row, which
  is itself an element of the $ab$-block $\operatorname{span}\{R(G_v^{ab}, q; (ab)\cdot)\}$
  (it equals $r\,i^* - w_{\mathrm{Other}}$, both terms in the block). By the per-edge block identity
  (\cref{lem:case-III-claim-6-11-redundant-row} infra) the $ab$-block is the
  $\mathrm{hingeRow}\,a\,b$-image of the $(D-1)$-dimensional hinge-row block
  $r(p(e_0)) = (\operatorname{span} C)^\perp$, so $w_{\mathrm{Gv}} = \mathrm{hingeRow}\,a\,b\,\rho$
  for some $\rho \in r(p(e_0))$. At the eq.~(6.12) seed $p_1 = q_0$ the shear identity
  $\mathrm{panelSupportExtensor}\,(n_a + t\,n_b)\,n_b = \mathrm{panelSupportExtensor}\,n_a\,n_b$
  (KT eq.~(6.16)) makes $\rho$ a hinge-row-block functional of $R(G, p_1)$'s $e_b = vb$-hinge too,
  so $\mathrm{hingeRow}\,v\,b\,\rho$ is the transported $(vb)i^*$-row, a genuine rigidity row of
  $R(G, p_1)$. Its eq.~(6.27) collapse against the inductive $(ab)$-part is the pure $(va)$-hinge
  candidate row $w$:
  \[ \mathrm{hingeRow}\,v\,b\,\rho \;-\; w_{\mathrm{Gv}} \;=\; \mathrm{hingeRow}\,v\,a\,\rho \;=\; w, \]
  by the hinge-difference collapse (\cref{lem:case-III-transport-collapse}).
\end{lemma}
\begin{proof}
  \leanok
  \uses{lem:case-III-transport-collapse}
  The $ab$-block element $w_{\mathrm{Gv}}$ is $(\mathrm{screwDiff}\,a\,b)^\vee\,\rho =
  \mathrm{hingeRow}\,a\,b\,\rho$ for some $\rho$ in the hinge-row block at $e_0$, by the block
  identity $\operatorname{span}\{R(G_v^{ab}, q; (ab)\cdot)\} = \mathrm{hingeRow}\,a\,b\,[r(p(e_0))]$.
  The same $\rho$ annihilates $C(p_1(e_b))$ because $C(p_1(e_b)) = C(q(e_0))$ by the shear identity,
  so $\mathrm{hingeRow}\,v\,b\,\rho$ is a rigidity row at the $vb$-link $e_b$. The collapse is the
  hinge-difference identity $(S_v - S_b) - (S_a - S_b) = S_v - S_a$.
\end{proof}

\begin{lemma}[The eq.\ (6.29)/(6.30)/(6.41) top-left $D \times D$ full rank, for \emph{at least one} candidate (the candidate-completion conditional); KT eqs.\ (6.29), Claim 6.12]
  \label{lem:case-III-eq629-conditional}
  \uses{lem:case-III-candidate-row-construction, lem:case-III-columnop,
        lem:case-II-placement-block-independent, lem:case-III-claim612}
  The conditional the candidate-completion assembly
  (\cref{lem:case-III-candidate-row}) passes through, discharged by Claim~6.12
  (\cref{lem:case-III-claim612}). At $d = 3$ ($D = 6$) this is \emph{not} a single-candidate
  fact: a single degenerate placement need not have a full-rank top-left block, so the conditional
  is the \emph{three-way disjunction} of KT's Claim~6.12. KT builds three candidate placements ---
  $p_1$ (split at $v$ along $va$, eqs.~(6.12)/(6.29)), $p_2$ (split at $v$ along $vb$,
  eqs.~(6.19)/(6.30)), and $p_3$ (split at the \emph{other} degree-2 vertex $a$ along $vc$,
  transported by the graph isomorphism $G_v^{ab} \cong G_a^{vc}$, eqs.~(6.32)/(6.41)). Each
  candidate $\rho \in \{\rho_{va}, \rho_{vb}, \rho_{ac}\}$ (the hinge-row-block functional its
  split contributes), together with a choice of $(d-2)$-affine lines $L \subset \Pi(a)$,
  $L' \subset \Pi(b)$, $L'' \subset \Pi(c)$, presents an augmented new block --- the candidate's
  $va$-block (resp.\ $vb$-, $ac$-) pinned rows together with the operated candidate row
  $w \circ \Phi \circ \mathrm{single}_v$ --- as a family of $D$ functionals on the splitting body's
  $D$-dimensional screw column $\mathrm{ScrewSpace}\ k$. The statement is the existential: for
  \emph{at least one} candidate $\rho$ and choice of lines, that $D$-functional family is linearly
  independent (the top-left $D \times D$ block of the block-triangularized $R(G, p_i)$ is full rank).
  This is exactly what \cref{lem:case-III-candidate-row}'s assembly consumes --- it takes
  $\rho$ / $\mathrm{rn}$ / $\mathrm{ro}$ abstractly, so the assembly itself stays green; only this
  conditional (which candidate to instantiate it at) is deferred. It is the one load-bearing
  hypothesis of the candidate-completion not yet discharged; the assembly is green \emph{modulo}
  this node.
\end{lemma}
\begin{proof}
  The three-way disjunction is Claim~6.12 (\cref{lem:case-III-claim612}, KT eqs.~(6.30)--(6.45)):
  if none of $M_1, M_2, M_3$ (the three top-left $D \times D$ submatrices) had full rank, the common
  vector $r := \sum_j \lambda_{(ab)j}\, r_j(q(ab))$ would lie in $(\operatorname{span} C(L))^\perp$,
  $(\operatorname{span} C(L'))^\perp$, $(\operatorname{span} C(L''))^\perp$ for all lines, hence
  annihilate the degree-$(d-1)$-extensor span~(6.45), which has full dimension $D$
  (\cref{lem:case-III-claim612-extensor-span}); so $r = 0$, contradicting $r \ne 0$
  (\cref{lem:case-III-claim612-r-nonzero}). See \cite[\S6.4.1]{katohTanigawa2011}.
\end{proof}

\begin{lemma}[The candidate-completion row operation: the missing $+1$ row $w$; KT eqs.\ (6.24)--(6.29)]
  \label{lem:case-III-candidate-row}
  \lean{CombinatorialRigidity.Molecular.BodyHingeFramework.linearIndependent_sum_augment_candidateRow}
  \leanok
  \uses{lem:case-III-claim-6-11, lem:case-III-redundant-decomposition,
        lem:case-III-candidate-row-construction,
        lem:case-III-vanish-off-column, lem:case-III-columnop, lem:case-III-seam,
        lem:case-III-transport-collapse,
        lem:case-III-conditional-block, lem:case-III-eq629-conditional,
        lem:case-II-realization-placement}
  Realize $G$ at the degenerate eq.~(6.12) placement $p_1$ with $p_1(vb) = q(ab)$
  (\cref{lem:case-II-realization-placement}). The redundant-$ab$-row of $R(G_v^{ab}, q)$,
  decomposed by \cref{lem:case-III-redundant-decomposition} (eqs.~(6.24)--(6.25)) into a $G_v$-row
  part and an other-$ab$-row part with $\lambda_{(ab)i^*} = 1$, is transported up to $R(G, p_1)$
  by the eq.~(6.26) row correspondence: the $(ab)j$-rows of $R(G_v^{ab}, q)$ map to the $(vb)j$-rows
  of $R(G, p_1)$ (KT eq.~(6.16)) and the $E_v$-rows correspond directly. After the KT eqs.~(6.14)--(6.15)
  \emph{column operation} (add column $v$ to column $a$), the resulting row
  $w = \sum_{1 \le j \le 5} \lambda_{(ab)j} R(G, p_1; (vb)j) + \sum_{e \in E_v, j}
  \lambda_{e_j} R(G, p_1; e_j)$ (eq.~(6.27)) has its $V \setminus \{v\}$ part all zero. By
  \cref{lem:case-III-vanish-off-column}, $w$ is then the pure $v$-column row
  $w = \bigl(\sum_j \lambda_{(ab)j}\, r_j(q(ab)),\ 0\bigr)$ of eq.~(6.28) (using $p_1(vb) = q(ab)$ and
  $R(G, p_1; E_v, v) = 0$). It is the new $(vb)i^*$-row that, joining the $va$-block, lifts $v$'s
  column contribution from $D - 1$ to $D$: the matrix $R(G, p_1)$ becomes block-triangular as in
  eq.~(6.29), with bottom-right $R(G_v^{ab} \setminus (ab)i^*, q)$ of rank $D(|V \setminus \{v\}| - 1)$.
\end{lemma}
\begin{proof}
  \leanok
  \uses{lem:case-III-candidate-row-construction, lem:case-III-columnop,
        lem:case-III-vanish-off-column, lem:case-III-transport-collapse,
        lem:case-III-conditional-block, lem:case-III-eq629-conditional,
        lem:case-II-realization-placement}
  \emph{Green modulo the eq.~(6.29) conditional (\cref{lem:case-III-eq629-conditional}, Claim~6.12
  territory).} The combination-level threading is green:
  \cref{lem:case-III-candidate-row-construction} takes the common element $w_{\mathrm{Gv}}$ of the
  eqs.~(6.24)--(6.25) decomposition (\cref{lem:case-III-redundant-decomposition}) --- the $G_v$-row
  part of the redundant $ab$-row, which is itself an $ab$-block element --- writes it as
  $w_{\mathrm{Gv}} = \mathrm{hingeRow}\ a\ b\ \rho$ for some $\rho \in r(p(e_0))$, certifies that the
  transported $(vb)i^*$-row $\mathrm{hingeRow}\ v\ b\ \rho$ is a genuine rigidity row of $R(G, p_1)$
  (the shear identity makes $\rho$ a hinge-row-block functional of the $e_b = vb$-hinge too), and
  collapses it to the candidate row $w = \mathrm{hingeRow}\ v\ a\ \rho$
  (\cref{lem:case-III-transport-collapse}). The row $w$ is supported on \emph{both} columns $v$ and
  $a$, with $w(S) = \rho(S_v - S_a)$; the column operation $\Phi(S) = \mathrm{update}\ S\ v\ (S_v +
  S_a)$ of eqs.~(6.14)--(6.15) (\cref{lem:case-III-columnop}) makes $w \circ \Phi$ pure $v$-column
  (\cref{lem:case-III-vanish-off-column}). \emph{The assembly} (this commit): precomposing the whole
  family $\mathrm{rn}\sqcup\{w\}\sqcup\mathrm{ro}$ with the automorphism $\Phi$ (the dual equivalence
  $\Phi^\vee$, preserving independence) yields the operated family, in which $w \circ \Phi$ joins the
  $va$-block $\mathrm{rn}$ in the pin-a-body augment (\cref{lem:case-III-conditional-block}) --- the
  old block $\mathrm{ro}$ is unchanged at the pin assignment $\mathrm{update}\ 0\ v\ x$ (since $\Phi$
  fixes it, $v \ne a$), so it keeps the brick's $\mathrm{hold}$ vanishing, and the $va$-rows keep
  their $v$-column pins. The augment is independent provided the eq.~(6.29) top-left $D \times D$
  block --- the $D - 1$ pinned $va$-rows plus the operated $w$'s $v$-column --- is full rank
  (\cref{lem:case-III-eq629-conditional}, the one \emph{conditional}, Claim~6.12 territory); then
  transporting the operated independence back through $\Phi^\vee$ gives the original family
  $\mathrm{rn}\sqcup\{w\}\sqcup\mathrm{ro}$ independent, of size
  $((D-1)+1) + D(|V_v|-1) = D(|V|-1)$ --- the full-rank assembly with bottom-right
  $R(G_v^{ab} \setminus (ab)i^*, q)$ of rank $D(|V_v|-1)$. The off-route eq.~(6.43)
  $a$-column vanishing (\cref{lem:case-III-acolumn-zero}) is a Claim~6.12 fact (the $M_3$ case), not
  used here.
\end{proof}

\subsubsection{The \texorpdfstring{$D$}{D}-candidate disjunction (KT eqs.\ (6.30)--(6.45))}
\label{sec:molecular-algebraic-induction-claim612}

The candidate-completion (\cref{sec:molecular-algebraic-induction-candidate-completion}) lifts a
\emph{single} degenerate placement $p_1$ to full rank $D(|V|-1)$ \emph{provided} its top-left
$D \times D$ block (eq.~(6.29)) is full rank (\cref{lem:case-III-eq629-conditional}). At $d = 2$ KT
show this holds for $p_1$ or its symmetric partner $p_2$; \emph{at $d = 3$ it may fail for both}, so
the conditional is genuinely a \emph{three-way disjunction}. KT introduce a third candidate $p_3$ ---
splitting off at the \emph{other} degree-2 vertex $a$ along $vc$, whose split-off graph $G_a^{vc}$ is
isomorphic to $G_v^{ab}$ (eqs.~(6.31)--(6.41)) --- and prove Claim~6.12: at least one of the three
top-left blocks $M_1, M_2, M_3$ (of eqs.~(6.29), (6.30), (6.41)) has full rank, for some choice of
lines $L \subset \Pi(a)$, $L' \subset \Pi(b)$, $L'' \subset \Pi(c)$ (writing $\Pi(u) := \Pi_{G_v^{ab},
q}(u)$). The argument is a clean contrapositive: if all three fail, a single nonzero $r \in \R^D$
$\bigl(r := \sum_j \lambda_{(ab)j}\, r_j(q(ab))\bigr)$ is orthogonal to every supporting extensor in
the union~(6.45), which Lemma~2.1 forces to span all of $\R^D$, so $r = 0$ --- a contradiction.

\begin{lemma}[The $D$ panel-support extensors of $4$ affinely-independent points span $\R^D$; KT eq.\ (6.45) via Lemma 2.1]
  \label{lem:case-III-claim612-extensor-span}
  \lean{CombinatorialRigidity.Molecular.span_omitTwoExtensor_eq_top}
  \leanok
  \uses{lem:extensor-independence, def:rigidity-matrix}
  At $d = 3$ ($D = 6$, $\mathrm{ScrewSpace}\,2 = \bigwedge^2 \R^4$, $\operatorname{finrank} = 6$),
  let $p_1, p_2, p_3, p_4 \in \R^3$ be affinely independent. The $\binom{4}{2} = 6$ panel-support
  $2$-extensors $C(p_i p_j) = p_i \vee p_j$ of the line through each pair span $\bigwedge^2 \R^4$:
  \[
    \operatorname{span}\{\, p_i \vee p_j \mid 1 \le i < j \le 4 \,\} = \bigwedge^2 \R^4
    \quad (\text{finrank } 6).
  \]
\end{lemma}
\begin{proof}
  \leanok
  By Lemma~2.1 (\cref{lem:extensor-independence}, at $e = 2$: $4$ affinely-independent points in
  $\R^3$) the six omit-two extensors $p_i \vee p_j$ are linearly independent in $\bigwedge^2 \R^4$;
  a linearly independent family of $6 = \operatorname{finrank} \bigwedge^2 \R^4$ vectors is a basis,
  so it spans.
\end{proof}

\begin{lemma}[A functional annihilating a spanning set is zero]
  \label{lem:case-III-claim612-orthseq-vanish}
  \lean{CombinatorialRigidity.Molecular.eq_zero_of_annihilates_span_top}
  \leanok
  \uses{lem:case-III-claim612-extensor-span}
  If a screw-space functional $r \in (\R^D)^* = \mathrm{Module.Dual}\,\R\,(\mathrm{ScrewSpace}\,k)$
  vanishes on every element of a set $S$ whose span is all of $\R^D$, then $r = 0$. (The
  non-degeneracy of the dual pairing on $\R^D$: a functional vanishing on a spanning set vanishes
  everywhere.)
\end{lemma}
\begin{proof}
  \leanok
  Two linear maps agreeing on a spanning set are equal (\texttt{LinearMap.ext\_on}); $r$ agrees with
  the zero functional on $S$, and $\operatorname{span} S = \R^D$, so $r = 0$.
\end{proof}

The contrapositive needs the union~(6.45) to span $\R^D$, and this in turn rests on
\emph{two} facts that the single old node bundled but that the capstone consumes separately:
(N3a) four \emph{affinely-independent} points $p_1, \dots, p_4$ realizing the
$\Pi(a)/\Pi(b)/\Pi(c)$ incidence pattern --- so that Lemma~2.1
(\cref{lem:case-III-claim612-extensor-span}) forces their six joins to span; and (N3b) the
\emph{point-join $\leftrightarrow$ panel-meet duality}, which turns the capstone's hypothesis
``$r \perp C(L)$ for every line $L$ in $\Pi(a) \cup \Pi(b) \cup \Pi(c)$'' into ``$r$
annihilates each spanning join $p_i \vee p_j$''. The first is a general-position
(configuration non-degeneracy) fact; the second --- the genuinely new content --- is
Grassmann--Cayley projective duality.

\begin{lemma}[Four affinely-independent points with the $\Pi(a)/\Pi(b)/\Pi(c)$ incidence pattern; KT eq.\ (6.45) point choice]
  \label{lem:case-III-claim612-points-affineIndep}
  \uses{def:rigidity-matrix, def:panel-hinge-framework}
  Let $(G_v^{ab}, q)$ be a panel-hinge framework at $d = 3$ in general position
  (\cref{def:panel-hinge-framework}: pairwise-independent panel normals $n_a, n_b, n_c$), with
  panel hyperplanes $\Pi(a), \Pi(b), \Pi(c) \subset \R^3$ at the three bodies $a, b, c$. Then
  there exist four \emph{affinely-independent} points $p \colon \mathrm{Fin}\,4 \to \R^3$
  realizing the triple-intersection incidence pattern
  \[
    p_1 \in \Pi(a) \cap \Pi(b) \cap \Pi(c), \quad
    p_2 \in \Pi(a) \cap \Pi(b) \setminus \Pi(c), \quad
    p_3 \in \Pi(b) \cap \Pi(c) \setminus \Pi(a), \quad
    p_4 \in \Pi(c) \cap \Pi(a) \setminus \Pi(b),
  \]
  where membership is the panel-incidence relation $p_i \in \Pi(u) \Leftrightarrow
  \langle \bar{p_i}, n_u \rangle = 0$ on the homogenization $\bar{p_i}$. The four points
  span an affine $3$-simplex; this is the affine independence \cref{lem:case-III-claim612-extensor-span}
  consumes (its six $2$-extensors $\bar{p_i} \vee \bar{p_j}$ then span $\R^D$).
\end{lemma}
\begin{proof}
  General position of the three panels suffices --- \emph{no} algebraic / transcendence assumption
  on the seed. With $n_a, n_b, n_c$ pairwise independent the three hyperplanes
  $\Pi(a), \Pi(b), \Pi(c)$ meet generically, so the triple intersection is a single affine point
  ($p_1$), each pairwise intersection is a line, and a point can be chosen on each pairwise line
  off the third panel ($p_2, p_3, p_4$); a determinant computation on the homogenized coordinates
  shows the four are affinely independent (KT p.~691). The incidence pattern is read off the
  three vanishing/non-vanishing conditions $\langle \bar{p_i}, n_u \rangle = 0$.
\end{proof}

\begin{lemma}[Point-join $\leftrightarrow$ panel-meet duality: a line in $\Pi(u)$ is annihilated by $r$ once $r \perp C(L)$; KT eq.\ (6.45) via Grassmann--Cayley]
  \label{lem:case-III-claim612-line-in-panel-union}
  \uses{def:join, def:meet, def:meet-complement-iso, def:panel-support-extensor,
        lem:extensor-independence, lem:case-III-claim612-extensor-span,
        lem:complement-iso-toDual, lem:complement-iso-decomposable-wedge-perp}
  At $d = 3$ ($D = 6$, $k = 2$, $\mathrm{ScrewSpace}\,2 = \bigwedge^2 \R^4$), let $p_i, p_j$ be two
  of the points of \cref{lem:case-III-claim612-points-affineIndep} whose connecting line
  $L = p_i p_j$ lies in a panel $\Pi(u)$ ($u \in \{a, b, c\}$). Then the point-join
  $\bar{p_i} \vee \bar{p_j} \in \bigwedge^2 \R^4$ and the panel-meet supporting extensor
  $C(L) = \mathrm{panelSupportExtensor}\ n_u\ (\cdot) = \mathrm{complementIso}(n_u \wedge \cdot)
  \in \bigwedge^2 \R^4$ of the same line are \emph{scalar multiples} of one another (both are the
  Pl\"ucker coordinate vector of the line $L$, up to the projective scale). Consequently a screw
  functional $r \in \mathrm{Module.Dual}\ \R\ (\mathrm{ScrewSpace}\,2)$ that annihilates
  \emph{every} panel-meet extensor $C(L)$ of lines $L \subset \Pi(a) \cup \Pi(b) \cup \Pi(c)$
  also annihilates each spanning join: $r(C(L)) = 0 \Rightarrow r(\bar{p_i} \vee \bar{p_j}) = 0$.
\end{lemma}
\begin{proof}
  Both $\bar{p_i} \vee \bar{p_j}$ and $C(L)$ are $2$-extensors in $\bigwedge^2 \R^4$
  representing the same projective line $L$. The join $\bar{p_i} \vee \bar{p_j}$
  (\cref{def:join}) is the line's supporting extensor written from \emph{two points on it}; the
  meet $C(L)$ (\cref{def:meet}, the complement iso \cref{def:meet-complement-iso} of the
  grade-$2$ join of two panel normals, \cref{def:panel-support-extensor}) is the same line's
  supporting extensor written from \emph{two hyperplanes through it}, and the Grassmann--Cayley
  duality of \cref{sec:molecular-meet} makes these two presentations of a $1$-dimensional
  projective subspace agree up to a nonzero scalar. The duality runs through the metric-free
  dictionary entry \cref{lem:complement-iso-toDual} (green): pairing $\mathrm{complementIso}$ of a
  decomposable $n_u \wedge n'$ against a $2$-extensor reduces to the volume form
  $\mathrm{vol}((n_u \wedge n') \wedge \cdot)$, so the incidence
  $\langle \bar{p_i}, n_u \rangle = \langle \bar{p_i}, n' \rangle = 0$ (and likewise for $\bar{p_j}$)
  puts both $\bar{p_i} \vee \bar{p_j}$ and $C(L)$ in the $1$-dimensional extensor line of $W = \{n_u,
  n'\}^{\perp} = \mathrm{span}\{\bar{p_i}, \bar{p_j}\}$ (nonzero precisely because $L$ is a genuine
  line). Step (i) --- $\mathrm{complementIso}(n_u \wedge n')$ lands in $\bigwedge^2 W$, the
  decomposable-of-orthogonal-complement step --- is now green
  (\cref{lem:complement-iso-decomposable-wedge-perp}): through the dictionary entry, a wedge with a
  shared factor has vanishing complement pairing, so $\mathrm{complementIso}(n_u \wedge n')$ is
  annihilated by every $2$-extensor sharing a vector with $n_u \wedge n'$. The one remaining step
  beyond the dictionary entry is (ii) $\bigwedge^2 W$ is $1$-dimensional for a $2$-dimensional $W$,
  forcing the two nonzero members to be scalar multiples $\bar{p_i} \vee \bar{p_j} = \lambda\, C(L)$.
  The annihilation transfer is then
  immediate: $r(C(L)) = 0$ gives $r(\bar{p_i} \vee \bar{p_j}) = \lambda\, r(C(L)) = 0$. This is the
  duality KT use implicitly when reading eq.~(6.45): the spanning family of
  \cref{lem:case-III-claim612-extensor-span} (point-joins) and the annihilated family of the
  contrapositive (panel-meets) are the \emph{same} extensors of the lines in the panel union.
\end{proof}

\begin{lemma}[Block full rank $\Leftrightarrow$ $\hat r \notin (\operatorname{span} C)^\perp$; KT (6.42) row-space criterion]
  \label{lem:case-III-claim612-block-iff-perp}
  \lean{CombinatorialRigidity.Molecular.BodyHingeFramework.linearIndependent_sumElim_candidateRow_iff,
        CombinatorialRigidity.Molecular.BodyHingeFramework.mem_hingeRowBlock_iff}
  \leanok
  \uses{def:rigidity-matrix, def:hinge-row-block, lem:case-II-placement-new-rows}
  Let the $D - 1$ rows of a candidate's hinge block span $(\operatorname{span} C)^\perp = r(p(e))$
  (the $D-1$-dimensional orthogonal complement of the line $\operatorname{span} C$ supporting the
  candidate's $(d-2)$-affine subspace $L$; \cref{lem:case-II-placement-new-rows}), and let
  $\hat r := \sum_j \lambda_{(ab)j}\, r_j(q(ab))$ be the candidate row. The augmented $D$-functional
  family ($D - 1$ block rows plus $\hat r$) is linearly independent iff
  \[
    \hat r \notin (\operatorname{span} C)^\perp \quad\Longleftrightarrow\quad \hat r(C) \ne 0,
  \]
  i.e.\ the candidate's top-left $D \times D$ block has full rank iff $\hat r$ is not orthogonal to
  the supporting extensor $C$.
\end{lemma}
\begin{proof}
  \leanok
  The $D - 1$ block rows already span the hyperplane $(\operatorname{span} C)^\perp$; appending
  $\hat r$ raises the dimension to $D$ iff $\hat r$ is fresh, i.e.\ $\hat r \notin
  (\operatorname{span} C)^\perp$ --- the row-space criterion for extending an independent family by
  one vector ($\mathrm{linearIndependent\_sumElim\_unit\_iff}$, else the family stays inside the
  $(D-1)$-dimensional span). Freshness off the hinge-row block is exactly $\hat r(C) \ne 0$ by the
  dual-annihilator $\perp\!\perp$ correspondence $\hat r \in (\operatorname{span} C)^\perp = r(p(e))
  \Leftrightarrow \hat r(C) = 0$ ($\mathrm{mem\_hingeRowBlock\_iff}$, the annihilator evaluated at
  the spanning $C$).
\end{proof}

\begin{lemma}[The common vector $r$ is nonzero; KT (6.42)]
  \label{lem:case-III-claim612-r-nonzero}
  \lean{CombinatorialRigidity.Molecular.BodyHingeFramework.candidateRow_ne_zero}
  \leanok
  \uses{lem:case-III-redundant-decomposition, lem:case-II-placement-new-rows}
  The common vector of the disjunction, $r := \sum_{1 \le j \le 5} \lambda_{(ab)j}\, r_j(q(ab))
  \in \R^D$, is nonzero. (The $r_j(q(ab))$ are the $D - 1$ linearly independent rows of the $ab$-hinge
  block, and the coefficient $\lambda_{(ab)i^*} = 1$ from the redundant-row decomposition of
  eq.~(6.25); a combination with a unit coefficient on a member of a linearly independent family is
  nonzero.)
\end{lemma}
\begin{proof}
  \leanok
  The $r_j(q(ab))$ ($1 \le j \le 5$, i.e.\ $D - 1$ of them) are linearly independent
  (\cref{lem:case-II-placement-new-rows}); by \cref{lem:case-III-redundant-decomposition} the
  coefficient $\lambda_{(ab)i^*} = 1$ (eq.~(6.25)). A linear combination of a linearly independent
  family with a nonzero coefficient is nonzero
  ($\mathrm{linearIndependent\_sum\_smul\_ne\_zero}$).
\end{proof}

\begin{lemma}[The $p_2$ placement: the symmetric $va \leftrightarrow vb$ candidate; KT eqs.\ (6.19)/(6.30)]
  \label{lem:case-III-claim612-p2-placement}
  \lean{CombinatorialRigidity.Molecular.BodyHingeFramework.linearIndependent_sum_p2_candidateRow}
  \leanok
  \uses{lem:case-II-realization-placement, lem:case-III-columnop,
        lem:case-III-claim612-block-iff-perp}
  The second candidate $p_2$: split off at $v$ along $vb$ (rather than $va$), the symmetric image of
  $p_1$ under $a \leftrightarrow b$. It reuses the eq.~(6.12) degenerate placement
  (\cref{lem:case-II-realization-placement}) with the roles of $a, b$ exchanged, and the column
  operation at $(v, b)$ (\cref{lem:case-III-columnop}) in place of $(v, a)$. By the same
  candidate-completion assembly (\cref{lem:case-III-candidate-row}, applied at the swapped roles),
  \emph{if} its top-left $D \times D$ block $M_2$ (eq.~(6.30)) is full rank --- equivalently
  $r \notin (\operatorname{span} C(L'))^\perp$, i.e.\ $r(C(L')) \ne 0$ for the line $L' \subset
  \Pi(b)$ (the row-space criterion \cref{lem:case-III-claim612-block-iff-perp}) --- then $R(G, p_2)$
  attains the full $D(|V|-1)$-size independent family. The same $\lambda_{(ab)j}$ and index $i^*$
  appear in $M_1$ and $M_2$ (they are determined by $(G_v^{ab}, q)$, independent of $p_1, p_2$).
\end{lemma}
\begin{proof}
  \leanok
  Verbatim symmetric to the $p_1$ construction under $a \leftrightarrow b$. The candidate-completion
  assembly (\cref{lem:case-III-candidate-row}) takes the splitting body $v$, its two endpoints, the
  candidate functional $\rho$, and the two row blocks abstractly, so applying it at the column op
  $\Phi = \mathrm{columnOp}\ (v \ne b)$ for the edge $vb$ in place of $va$ is the same lemma. Its one
  hypothesis --- the operated, $v$-pinned top-left block being full rank --- is supplied by the
  row-space criterion (\cref{lem:case-III-claim612-block-iff-perp}) at the $vb$-hinge once the
  operated, pinned candidate row $(\mathrm{hingeRow}\ v\ b\ \rho) \circ \Phi \circ \mathrm{single}_v$
  is identified with $\rho$ itself (the column op makes it pure $v$-column with value $\rho(S_v)$),
  which holds iff $\rho(C) \ne 0$. The redundant-row combination is unchanged: it lives in
  $R(G_v^{ab}, q)$, common to both candidates.
\end{proof}

\begin{lemma}[The $p_3$ placement: the third candidate via $G_v^{ab} \cong G_a^{vc}$; KT eqs.\ (6.31)--(6.41)]
  \label{lem:case-III-claim612-p3-placement}
  \lean{CombinatorialRigidity.Molecular.BodyHingeFramework.linearIndependent_sum_p3_candidateRow}
  \leanok
  \uses{lem:case-II-realization-placement, lem:case-III-columnop,
        lem:case-III-claim612-block-iff-perp, lem:case-III-claim612-eq644}
  The third candidate $p_3$, available because $a$ too is a degree-2 vertex: split off at $a$ along
  $vc$. The resulting graph $G_a^{vc}$ is isomorphic to $G_v^{ab}$ by $\rho \colon V \setminus \{a\}
  \to V \setminus \{v\}$, $\rho(v) = a$, $\rho(u) = u$ otherwise; the induced placement
  $q_\rho$ satisfies $p_3(va) = q(ac)$, $p_3(vb) = q(ab)$, $p_3(ac) = L''$, $p_3(e) = q(e)$ otherwise
  (eq.~(6.33)). The column operation at $(a, c)$ ($\mathrm{col}_a \mathrel{+}= \mathrm{col}_c$,
  eq.~(6.35)) block-triangularizes $R(G, p_3)$ to eq.~(6.41), whose top-left $D \times D$ block is
  $M_3$ with rows $r(p_3(ac)) = r(L'')$ and $\sum_j \lambda_{(ac)j}\, r_j(q(ac))$, full rank iff
  $r \notin (\operatorname{span} C(L''))^\perp$ for $L'' \subset \Pi(c)$ --- using eq.~(6.44)
  (\cref{lem:case-III-claim612-eq644}) to route the $M_3$ row onto the same $r$.
\end{lemma}
\begin{proof}
  \leanok
  The isomorphism $\rho$ identifies $(G_a^{vc}, q_\rho)$ with $(G_v^{ab}, q)$ as panel-hinge
  frameworks (eq.~(6.31)), so $\Pi_{G_a^{vc}, q_\rho}(u) = \Pi(u)$ for $u \ne v, a$ and
  $\Pi_{G_a^{vc}, q_\rho}(v) = \Pi(a)$. Formally this is invisible: the candidate-completion assembly
  (\cref{lem:case-III-candidate-row}) takes the splitting body, its two endpoints, the candidate
  functional $\rho$, and the two row blocks abstractly, so applying it at the column op $\Phi =
  \mathrm{columnOp}\ (a \ne c)$ for the edge $ac$ --- the split body now $a$, its operated endpoint
  $c$ --- is the same lemma; no graph swap is needed, and the iso is handled functionally through
  the candidate row (verbatim symmetric to the $p_2$ construction under the role change
  $v, b \mapsto a, c$). Its one hypothesis --- the operated, $a$-pinned top-left block being full
  rank --- is supplied by the row-space criterion (\cref{lem:case-III-claim612-block-iff-perp}) at
  the $ac$-hinge once the operated, pinned candidate row
  $(\mathrm{hingeRow}\ a\ c\ \rho) \circ \Phi \circ \mathrm{single}_a$ is identified with $\rho$
  itself (the column op makes it pure $a$-column with value $\rho(S_a)$), which holds iff
  $\rho(C) \ne 0$. The column op at $(a,c)$ (eq.~(6.35)$\to$(6.36)) and the substitution
  $p_3(va) = q(ac)$, $p_3(vb) = q(ab)$ (eq.~(6.34)) reduce $R(G, p_3)$ to eq.~(6.41) by the same row
  operations as for $R(G, p_1)$ (the eq.~(6.40) row reduction). The eq.~(6.44) identity
  (\cref{lem:case-III-claim612-eq644}) gives $M_3$'s row as $-\sum_j \lambda_{(ac)j}\, r_j(q(ac)) =
  r$ (up to sign), so the capstone reads its full-rank criterion off the same common vector $r$.
\end{proof}

\begin{lemma}[Eq.\ (6.44): the $M_3$ row equals $-r$, routing $M_3$ onto the same $r$; KT eq.\ (6.44)]
  \label{lem:case-III-claim612-eq644}
  \lean{CombinatorialRigidity.Molecular.BodyHingeFramework.candidateRow_ac_eq_neg,
        CombinatorialRigidity.Molecular.BodyHingeFramework.hingeRow_comp_single_tail,
        CombinatorialRigidity.Molecular.BodyHingeFramework.hingeRow_comp_single_off}
  \leanok
  \uses{lem:case-III-acolumn-zero, lem:case-III-redundant-decomposition}
  In $G_v^{ab}$ only $ab$ and $ac$ are incident to the body $a$, so the $a$-column block of the
  eq.~(6.24) vanishing combination (\cref{lem:case-III-acolumn-zero}, eq.~(6.43)) reads
  \[
    0 = \sum_{1 \le j \le 5} \lambda_{(ab)j}\, r_j(q(ab)) + \sum_{1 \le j \le 5} \lambda_{(ac)j}\,
        r_j(q(ac)),
  \]
  which, with $r = \sum_j \lambda_{(ab)j}\, r_j(q(ab))$, gives KT's eq.~(6.44):
  \[
    r = -\sum_{1 \le j \le 5} \lambda_{(ac)j}\, r_j(q(ac)).
  \]
  Hence the $M_3$ candidate row $\sum_j \lambda_{(ac)j}\, r_j(q(ac))$ equals $-r$, routing the
  third candidate's full-rank criterion onto the \emph{same} common vector $r$ as $M_1, M_2$.
\end{lemma}
\begin{proof}
  \leanok
  Eq.~(6.43) (\cref{lem:case-III-acolumn-zero}) is the $a$-column restriction of the eq.~(6.24)
  vanishing combination; since only the $ab$- and $ac$-edges are incident to $a$ in $G_v^{ab}$
  (every other $R(G_v^{ab}, q; e_j, a) = 0$ by the rigidity-matrix definition --- the
  column-restriction leaves $\mathrm{hingeRow\_comp\_single\_tail}$ for the $ab$/$ac$-rows and
  $\mathrm{hingeRow\_comp\_single\_off}$ for every other edge), the surviving terms are exactly
  the $ab$- and $ac$-sums. Rearranging $\sum_j \lambda_{(ab)j}\, r_j(q(ab)) + \sum_j
  \lambda_{(ac)j}\, r_j(q(ac)) = 0$ for $r$ is eq.~(6.44).
\end{proof}

\begin{lemma}[Claim 6.12: at least one of $M_1, M_2, M_3$ has full rank; KT Claim 6.12, eqs.\ (6.30)--(6.45)]
  \label{lem:case-III-claim612}
  \uses{lem:case-III-claim612-extensor-span, lem:case-III-claim612-orthseq-vanish,
        lem:case-III-claim612-points-affineIndep, lem:case-III-claim612-line-in-panel-union,
        lem:case-III-claim612-block-iff-perp,
        lem:case-III-claim612-r-nonzero, lem:case-III-claim612-p2-placement,
        lem:case-III-claim612-p3-placement, lem:case-III-claim612-eq644}
  At $d = 3$, for the three candidate placements $p_1, p_2, p_3$
  (\cref{lem:case-II-realization-placement}, \cref{lem:case-III-claim612-p2-placement},
  \cref{lem:case-III-claim612-p3-placement}), at least one of the top-left $D \times D$ submatrices
  $M_1, M_2, M_3$ (of eqs.~(6.29), (6.30), (6.41)) has full rank, for some choice of
  $(d-2)$-affine lines $L \subset \Pi(a)$, $L' \subset \Pi(b)$, $L'' \subset \Pi(c)$. Equivalently,
  at least one of $R(G, p_1), R(G, p_2), R(G, p_3)$ attains the desired rank $D(|V| - 1)$, discharging
  the candidate-completion conditional (\cref{lem:case-III-eq629-conditional}).
\end{lemma}
\begin{proof}
  Contrapositive. Suppose all three blocks fail to have full rank for \emph{every} choice of lines.
  By the row-space criterion (\cref{lem:case-III-claim612-block-iff-perp}), $M_1$ failing places the
  common vector $r := \sum_j \lambda_{(ab)j}\, r_j(q(ab))$ in $(\operatorname{span} C(L))^\perp$ for
  all $L \subset \Pi(a)$; likewise $M_2$ failing puts $r \in (\operatorname{span} C(L'))^\perp$ for
  all $L' \subset \Pi(b)$. For $M_3$, eq.~(6.44) (\cref{lem:case-III-claim612-eq644}) makes its
  candidate row $\sum_j \lambda_{(ac)j}\, r_j(q(ac)) = -r$, so $M_3$ failing puts the \emph{same} $r$
  in $(\operatorname{span} C(L''))^\perp$ for all $L'' \subset \Pi(c)$. Hence $r$ is orthogonal to
  every panel-meet supporting extensor $C(L)$ in the union~(6.45),
  $\bigl(\bigcup_{L \subset \Pi(a)} C(L)\bigr) \cup \bigl(\bigcup_{L' \subset \Pi(b)} C(L')\bigr)
  \cup \bigl(\bigcup_{L'' \subset \Pi(c)} C(L'')\bigr)$. Take the four affinely-independent points
  $p_1, p_2, p_3, p_4$ of \cref{lem:case-III-claim612-points-affineIndep}: each connecting line
  $p_i p_j$ lies in $\Pi(a) \cup \Pi(b) \cup \Pi(c)$, so by the point-join $\leftrightarrow$
  panel-meet duality (\cref{lem:case-III-claim612-line-in-panel-union}) the join $p_i \vee p_j$ is a
  scalar multiple of that line's panel-meet $C(L)$, hence $r$ annihilates each $p_i \vee p_j$ too.
  But the six joins span $\R^D$ (\cref{lem:case-III-claim612-extensor-span}, Lemma~2.1 on the four
  affinely-independent points). So $r$ annihilates a spanning set and $r = 0$
  (\cref{lem:case-III-claim612-orthseq-vanish}), contradicting $r \ne 0$
  (\cref{lem:case-III-claim612-r-nonzero}). Therefore at least one block has full rank.
\end{proof}

\begin{lemma}[Case III: $k = 0$, no proper rigid subgraph; KT Lemma 6.10/6.13]
  \label{lem:case-III}
  \uses{thm:minimal-kdof-reduction, lem:extensor-independence,
        lem:cycle-realization, lem:case-II-realization-placement,
        lem:case-III-claim-6-11-base, lem:case-III-gap3-minimalKDof,
        lem:case-III-seed-rank-bridge, lem:case-III-seed-rank-upper,
        lem:case-III-rank-attainment, lem:case-III-claim-6-11-redundant-row,
        lem:case-III-claim-6-11, lem:case-III-redundant-decomposition,
        lem:case-III-acolumn-zero, lem:case-III-candidate-row,
        lem:case-III-candidate-row-construction, lem:case-III-conditional-block,
        lem:case-III-eq629-conditional, lem:case-III-transport-collapse,
        lem:case-III-vanish-off-column, lem:case-III-columnop, lem:case-III-seam,
        lem:case-III-claim612}
  Let $G$ be a minimal $0$-dof-graph that is $2$-edge-connected with no
  proper rigid subgraph. Then $G$ has a panel-hinge realization at rank
  $D(|V|-1)$. A single candidate realization is one row short of full
  rank (the stratum-1 brick $D(|V|-1) - 1$ of
  \cref{lem:case-II-realization-placement}); the missing $+1$ row is KT
  Claim~6.11 (\cref{lem:case-III-claim-6-11}), now green: its matroid-base
  4.3(ii) form (\cref{lem:case-III-claim-6-11-base}), the nested-IH reduction
  $G - v$ minimal $k'$-dof with $k' \le D - 2$
  (\cref{lem:case-III-gap3-minimalKDof}), the analytic
  rank-at-the-fixed-seed attainment (\cref{lem:case-III-seed-rank-bridge} for
  $\operatorname{def} = 0$, its $\operatorname{def} > 0$ upper-bound half
  \cref{lem:case-III-seed-rank-upper}, and their packaging into the exact rank
  \cref{lem:case-III-rank-attainment}), and the redundant-row pigeonhole
  (\cref{lem:case-III-claim-6-11-redundant-row}) fed through the row-set
  identity at the specific graphs $G_v^{ab}$, $G_v$ all assemble into the
  redundant $ab$-row. The candidate-completion
  (\cref{sec:molecular-algebraic-induction-candidate-completion}) converts that
  redundant $ab$-row into the missing $+1$: the row operation of eqs.~(6.24)--(6.29)
  (\cref{lem:case-III-candidate-row}, green modulo the eq.~(6.29) conditional) produces a pure
  $v$-column row $w$ and assembles it with the $va$-block and old block into the
  $D(|V|-1)$-size independent family (\cref{lem:case-III-conditional-block}), block-triangularizing
  $R(G, p_1)$ as in eq.~(6.29). The one conditional that assembly passes through
  (\cref{lem:case-III-eq629-conditional}, the top-left $D \times D$ full rank for at least one
  candidate) is discharged by the $D$-candidate disjunction of Claim~6.12
  (\cref{lem:case-III-claim612}, KT eqs.~(6.30)--(6.45),
  \cref{sec:molecular-algebraic-induction-claim612}): at $d = 3$ KT build \emph{three} candidate
  realizations $p_1, p_2, p_3$ (splitting at $v$ along $va$/$vb$, and at the other degree-2 vertex
  $a$ along $vc$ via $G_v^{ab} \cong G_a^{vc}$), and show at least one has a full-rank top-left
  block. The degree-2 condition forces all three to test the \emph{same} vector $r =
  \sum_j \lambda_{(ab)j}\, r_j(q(ab)) \in \R^D$ (eq.~(6.44),
  \cref{lem:case-III-claim612-eq644}); if all failed, $r$ would annihilate every panel-meet
  $C(L)$ of lines in $\Pi(a) \cup \Pi(b) \cup \Pi(c)$, hence --- by the point-join $\leftrightarrow$
  panel-meet duality (\cref{lem:case-III-claim612-line-in-panel-union}) --- the
  $\binom{4}{2} = D$ joins of four affinely-independent points
  (\cref{lem:case-III-claim612-points-affineIndep}), which by the extensor-independence Lemma~2.1
  (\cref{lem:extensor-independence}, via \cref{lem:case-III-claim612-extensor-span}) span $\R^D$,
  forcing $r = 0$ --- contradicting $r \ne 0$ (\cref{lem:case-III-claim612-r-nonzero}).
  \textbf{Deferred to Phases~22--23} ($d = 3$ first, then general $d$); stated
  here only to close the Theorem~5.5 dichotomy. The Claim~6.12 nodes
  (\cref{sec:molecular-algebraic-induction-claim612}) are the $d = 3$ remainder, cut into
  buildable leaves.
\end{lemma}
\begin{proof}
  Deferred to Phases~22--23. See \cite[\S6.4]{katohTanigawa2011}.
\end{proof}
